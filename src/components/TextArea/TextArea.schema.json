{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "TextArea",
  "description": "A multi-line text input with label, validation states, character count, auto-resize, clearable, and icon support.",
  "type": "object",
  "properties": {
    "label": {
      "type": "object",
      "description": "React.ReactNode — Label displayed above the textarea."
    },
    "description": {
      "type": "object",
      "description": "React.ReactNode — Description text below the label."
    },
    "error": {
      "type": "boolean",
      "description": "Whether the textarea is in an error state.",
      "default": false
    },
    "errorMessage": {
      "type": "object",
      "description": "React.ReactNode — Error message displayed below the textarea."
    },
    "success": {
      "type": "boolean",
      "description": "Green success state for validated fields.",
      "default": false
    },
    "successMessage": {
      "type": "object",
      "description": "React.ReactNode — Success message displayed below the textarea."
    },
    "size": {
      "type": "string",
      "enum": [
        "sm",
        "md",
        "lg"
      ],
      "description": "Visual size variant. Emits data-size on the root container for CSS targeting."
    },
    "startIcon": {
      "type": "object",
      "description": "React.ReactNode — Icon rendered before the textarea."
    },
    "endIcon": {
      "type": "object",
      "description": "React.ReactNode — Icon rendered after the textarea."
    },
    "onStartIconClick": {
      "type": "object",
      "description": "() => void — Makes startIcon a clickable button."
    },
    "onEndIconClick": {
      "type": "object",
      "description": "() => void — Makes endIcon a clickable button."
    },
    "startIconLabel": {
      "type": "string",
      "description": "Accessible label for startIcon when clickable. Required with onStartIconClick."
    },
    "endIconLabel": {
      "type": "string",
      "description": "Accessible label for endIcon when clickable. Required with onEndIconClick."
    },
    "onValueChange": {
      "type": "object",
      "description": "(value: string) => void — Fires with the current string value when the value changes."
    },
    "clearable": {
      "type": "boolean",
      "description": "Show a built-in clear button when the textarea has a value.",
      "default": false
    },
    "onClear": {
      "type": "object",
      "description": "() => void — Callback when the clear button is clicked."
    },
    "showCount": {
      "type": "boolean",
      "description": "Show character count (requires maxLength to be set).",
      "default": false
    },
    "autoResize": {
      "type": "boolean",
      "description": "Automatically resize height to fit content.",
      "default": false
    },
    "loading": {
      "type": "boolean",
      "description": "Whether the textarea is in a loading state.",
      "default": false
    },
    "loader": {
      "type": "object",
      "description": "React.ReactNode — Custom loader element."
    },
    "loaderSize": {
      "type": "number",
      "description": "Size of the default CircularLoader in pixels.",
      "default": 16
    },
    "fullWidth": {
      "type": "boolean",
      "description": "Whether the textarea expands to fill its parent container.",
      "default": false
    },
    "className": {
      "type": "string",
      "description": "CSS class for the root container element."
    },
    "classes": {
      "type": "object",
      "description": "Slot-based class overrides for internal elements.",
      "properties": {
        "root": {
          "type": "string"
        },
        "wrapper": {
          "type": "string"
        },
        "label": {
          "type": "string"
        },
        "description": {
          "type": "string"
        },
        "textarea": {
          "type": "string"
        },
        "error": {
          "type": "string"
        },
        "success": {
          "type": "string"
        },
        "count": {
          "type": "string"
        }
      }
    },
    "unstyled": {
      "type": "boolean",
      "description": "When true, all default classes are removed; only classes overrides apply.",
      "default": false
    },
    "placeholder": {
      "type": "string",
      "description": "Placeholder text for the textarea."
    },
    "disabled": {
      "type": "boolean",
      "description": "Whether the textarea is disabled.",
      "default": false
    },
    "required": {
      "type": "boolean",
      "description": "Whether the field is required.",
      "default": false
    },
    "name": {
      "type": "string",
      "description": "Form field name attribute."
    },
    "id": {
      "type": "string",
      "description": "HTML id attribute."
    },
    "rows": {
      "type": "number",
      "description": "Number of visible text rows.",
      "default": 4
    },
    "maxLength": {
      "type": "number",
      "description": "Maximum number of characters allowed."
    },
    "value": {
      "type": "string",
      "description": "Controlled value of the textarea."
    },
    "defaultValue": {
      "type": "string",
      "description": "Default uncontrolled value."
    }
  },
  "required": []
}
