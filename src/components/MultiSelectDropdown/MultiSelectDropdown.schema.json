{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "MultiSelectDropdown",
  "description": "A multi-select dropdown with chip display, keyboard navigation, async loading, and custom triggers.",
  "type": "object",
  "properties": {
    "options": {
      "type": "array",
      "description": "List of selectable options.",
      "items": {
        "type": "object",
        "properties": {
          "value": { "type": "string", "description": "Option value." },
          "label": { "type": "string", "description": "Display label." },
          "content": { "type": "object", "description": "React.ReactNode - Custom content rendered in the option list." },
          "selectedContent": { "type": "object", "description": "React.ReactNode - Content shown when this option is selected." },
          "disabled": { "type": "boolean", "description": "Whether the option is disabled." }
        },
        "required": ["value", "label"]
      }
    },
    "value": {
      "type": "array",
      "description": "Controlled selected values.",
      "items": { "type": "string" }
    },
    "defaultValue": {
      "type": "array",
      "description": "Initial selected values for uncontrolled usage.",
      "items": { "type": "string" }
    },
    "onValueChange": {
      "type": "object",
      "description": "(values: string[], options: MultiSelectOption[]) => void - Fires when the selected values change."
    },
    "id": {
      "type": "string",
      "description": "HTML id attribute for the trigger element."
    },
    "name": {
      "type": "string",
      "description": "Form field name."
    },
    "placeholder": {
      "type": "object",
      "description": "React.ReactNode - Placeholder when nothing is selected."
    },
    "disabled": {
      "type": "boolean",
      "description": "Disables the dropdown."
    },
    "error": {
      "type": "boolean",
      "description": "Displays the dropdown in an error state."
    },
    "errorMessage": {
      "type": "object",
      "description": "React.ReactNode - Error message displayed below the dropdown."
    },
    "label": {
      "type": "object",
      "description": "React.ReactNode - Label rendered above the dropdown."
    },
    "description": {
      "type": "object",
      "description": "React.ReactNode - Description text rendered below the label."
    },
    "success": {
      "type": "boolean",
      "description": "Displays the dropdown in a success state."
    },
    "successMessage": {
      "type": "object",
      "description": "React.ReactNode - Success message displayed below the dropdown."
    },
    "required": {
      "type": "boolean",
      "description": "Marks the field as required."
    },
    "noResultsContent": {
      "type": "object",
      "description": "React.ReactNode - Content shown when no options are available."
    },
    "clearable": {
      "type": "boolean",
      "description": "Shows a clear button to deselect all values."
    },
    "showChevron": {
      "type": "boolean",
      "description": "Whether to show the chevron icon in the trigger."
    },
    "fullWidth": {
      "type": "boolean",
      "description": "Makes the dropdown span the full width of its container."
    },
    "loading": {
      "type": "boolean",
      "description": "Shows a loading/shimmer state."
    },
    "onLoadOptions": {
      "type": "object",
      "description": "() => Promise<MultiSelectOption[]> - Async function to load options."
    },
    "loadOnOpen": {
      "type": "boolean",
      "description": "When true, calls onLoadOptions each time the dropdown opens."
    },
    "onLoadError": {
      "type": "object",
      "description": "(error: unknown) => void - Called when async option loading fails."
    },
    "shimmerCount": {
      "type": "number",
      "description": "Number of shimmer placeholder items shown during loading."
    },
    "maxDisplayedChips": {
      "type": "number",
      "description": "Maximum number of selected chips displayed before showing a count."
    },
    "showSelectedChips": {
      "type": "boolean",
      "description": "Whether to show selected values as chips in the trigger."
    },
    "checkboxIcon": {
      "type": "object",
      "description": "React.ReactNode - Custom checkbox icon for options."
    },
    "unstyled": {
      "type": "boolean",
      "description": "Removes all default styling."
    },
    "lockScroll": {
      "type": "boolean",
      "description": "Locks body scroll while the dropdown is open."
    },
    "classes": {
      "type": "object",
      "description": "CSS class overrides for dropdown sub-elements.",
      "properties": {
        "root": { "type": "string" },
        "wrapper": { "type": "string" },
        "trigger": { "type": "string" },
        "triggerText": { "type": "string" },
        "content": { "type": "string" },
        "optionList": { "type": "string" },
        "option": { "type": "string" },
        "optionSelected": { "type": "string" },
        "optionFocused": { "type": "string" },
        "optionDisabled": { "type": "string" },
        "chevron": { "type": "string" },
        "checkbox": { "type": "string" },
        "checkboxChecked": { "type": "string" },
        "checkboxIcon": { "type": "string" },
        "chip": { "type": "string" },
        "chipRemove": { "type": "string" },
        "noResults": { "type": "string" },
        "label": { "type": "string" },
        "error": { "type": "string" },
        "description": { "type": "string" },
        "success": { "type": "string" },
        "clearIcon": { "type": "string" },
        "shimmer": { "type": "string" },
        "shimmerItem": { "type": "string" },
        "moreCount": { "type": "string" }
      }
    },
    "className": {
      "type": "string",
      "description": "Additional CSS class for the root element."
    },
    "style": {
      "type": "object",
      "description": "Inline styles for the root element."
    },
    "open": {
      "type": "boolean",
      "description": "Controlled open state."
    },
    "defaultOpen": {
      "type": "boolean",
      "description": "Whether the dropdown is initially open (uncontrolled)."
    },
    "onOpenChange": {
      "type": "object",
      "description": "(open: boolean) => void - Fires when the open state changes."
    },
    "keepMounted": {
      "type": "boolean",
      "description": "Keeps the dropdown DOM mounted when closed."
    },
    "portalContainer": {
      "type": "object",
      "description": "HTMLElement | null - Portal target for the dropdown popup."
    },
    "dropdownPosition": {
      "type": "string",
      "enum": ["top", "bottom"],
      "description": "Vertical placement of the dropdown relative to the trigger."
    },
    "forceDropdownPosition": {
      "type": "boolean",
      "description": "Locks the dropdown to the specified position without auto-flipping."
    },
    "dropdownZIndex": {
      "type": "number",
      "description": "Z-index of the dropdown popup."
    },
    "dropdownGap": {
      "type": "number",
      "description": "Gap in pixels between trigger and dropdown popup."
    },
    "aria-label": {
      "type": "string",
      "description": "Accessible label for the dropdown."
    },
    "onBlur": {
      "type": "object",
      "description": "() => void - Fires when the dropdown loses focus."
    },
    "onFocus": {
      "type": "object",
      "description": "() => void - Fires when the dropdown receives focus."
    },
    "onKeyDown": {
      "type": "object",
      "description": "(event: React.KeyboardEvent) => void - Fires on keydown events."
    },
    "ChevronIcon": {
      "type": "object",
      "description": "React.ComponentType<{ className?: string; style?: CSSProperties }> - Custom chevron icon component."
    },
    "loadingText": {
      "type": "object",
      "description": "React.ReactNode - Text shown during async loading."
    },
    "ClearIcon": {
      "type": "object",
      "description": "React.ComponentType<{ className?: string }> - Custom clear icon component."
    },
    "renderTrigger": {
      "type": "object",
      "description": "(props: MultiSelectDropdownTriggerRenderProps) => ReactNode - Custom render function for the trigger element."
    }
  },
  "required": []
}
