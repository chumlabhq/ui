{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Table",
  "description": "A feature-rich data table with sorting, filtering, selection, expansion, pinning, inline editing, infinite scroll, and responsive mobile card layout.",
  "type": "object",
  "properties": {
    "columns": {
      "type": "array",
      "description": "TanStack ColumnDef array defining table columns."
    },
    "data": {
      "type": "array",
      "description": "Array of row data objects."
    },
    "COLUMNS": {
      "type": "array",
      "description": "Deprecated alias for columns."
    },
    "COLUMNS_DATA": {
      "type": "array",
      "description": "Deprecated alias for data."
    },
    "loading": {
      "type": "boolean",
      "description": "Whether the table is in a loading state showing shimmer rows.",
      "default": false
    },
    "showHeader": {
      "type": "boolean",
      "description": "Whether to show the table header row.",
      "default": true
    },
    "tableHeader": {
      "type": "boolean",
      "description": "Legacy alias for showHeader."
    },
    "pinnedColumns": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Column IDs to pin to the left side."
    },
    "onPinColumn": {
      "type": "object",
      "description": "(columnId: string, isPinned: boolean) => void — Callback when a column is pinned or unpinned."
    },
    "pinnableColumns": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Column IDs that are allowed to be pinned."
    },
    "maxPinnedColumns": {
      "type": "number",
      "description": "Maximum number of columns that can be pinned simultaneously.",
      "default": 3
    },
    "onCursorPosition": {
      "type": "object",
      "description": "(position: { x: number; y: number } | null) => void — Callback with cursor position over the table."
    },
    "onCursorOverHeader": {
      "type": "object",
      "description": "(isOverHeader: boolean) => void — Callback when cursor enters/leaves the header."
    },
    "emptyContent": {
      "type": "object",
      "description": "React.ReactNode — Content shown when the table has no data."
    },
    "ariaLabel": {
      "type": "string",
      "description": "Accessible label for the table.",
      "default": "Data table"
    },
    "caption": {
      "type": "string",
      "description": "Accessible caption rendered as a visually hidden <caption> element."
    },
    "getRowId": {
      "type": "object",
      "description": "(row: TData) => string — Function to derive a unique row ID."
    },
    "selectedRowId": {
      "type": [
        "string",
        "null"
      ],
      "description": "ID of the currently selected row (single selection)."
    },
    "onRowClick": {
      "type": "object",
      "description": "(rowData: TData) => void — Callback fired when a row is clicked."
    },
    "onRowHover": {
      "type": "object",
      "description": "(rowIndex: number | null, rowRef?: React.RefObject) => void — Callback fired when a row is hovered."
    },
    "floatingActions": {
      "type": "object",
      "description": "React.ReactNode — Floating action elements displayed on row hover."
    },
    "isFloatingActionsHovered": {
      "type": "boolean",
      "description": "Whether the floating actions area is being hovered.",
      "default": false
    },
    "isPopupOpen": {
      "type": "boolean",
      "description": "Whether a popup triggered from the table is currently open.",
      "default": false
    },
    "maxWidth": {
      "type": [
        "string",
        "number"
      ],
      "description": "Maximum width of the table container."
    },
    "maxHeight": {
      "type": [
        "string",
        "number"
      ],
      "description": "Maximum height of the table container."
    },
    "minHeight": {
      "type": [
        "string",
        "number"
      ],
      "description": "Minimum height of the table container."
    },
    "hideVerticalScrollbar": {
      "type": "boolean",
      "description": "Whether to hide the vertical scrollbar.",
      "default": false
    },
    "hideHorizontalScrollbar": {
      "type": "boolean",
      "description": "Whether to hide the horizontal scrollbar.",
      "default": false
    },
    "stickyHeader": {
      "type": "boolean",
      "description": "Whether the header row sticks to the top when scrolling.",
      "default": false
    },
    "classes": {
      "type": "object",
      "description": "CSS class overrides for table sub-elements.",
      "properties": {
        "container": {
          "type": "string"
        },
        "table": {
          "type": "string"
        },
        "header": {
          "type": "string"
        },
        "headerRow": {
          "type": "string"
        },
        "headerCell": {
          "type": "string"
        },
        "headerCellHover": {
          "type": "string"
        },
        "headerCellContent": {
          "type": "string"
        },
        "body": {
          "type": "string"
        },
        "row": {
          "type": "string"
        },
        "cell": {
          "type": "string"
        },
        "selectedRow": {
          "type": "string"
        },
        "pinnedContainer": {
          "type": "string"
        },
        "pinnedTable": {
          "type": "string"
        },
        "unpinnedContainer": {
          "type": "string"
        },
        "unpinnedTable": {
          "type": "string"
        },
        "empty": {
          "type": "string"
        },
        "pinIcon": {
          "type": "string"
        },
        "pinIconHover": {
          "type": "string"
        },
        "pinnedPinIcon": {
          "type": "string"
        },
        "pinnedPinIconHover": {
          "type": "string"
        },
        "pinButton": {
          "type": "string"
        },
        "pinnedPinButton": {
          "type": "string"
        },
        "shimmer": {
          "type": "string"
        },
        "shimmerRow": {
          "type": "string"
        },
        "shimmerCell": {
          "type": "string"
        },
        "shimmerBar": {
          "type": "string"
        },
        "checkbox": {
          "type": "string"
        },
        "checkboxCell": {
          "type": "string"
        },
        "sortIcon": {
          "type": "string"
        },
        "filterInput": {
          "type": "string"
        },
        "filterIcon": {
          "type": "string"
        },
        "filterDropdown": {
          "type": "string"
        },
        "expandIcon": {
          "type": "string"
        },
        "expandedRow": {
          "type": "string"
        },
        "pinnedRightContainer": {
          "type": "string"
        },
        "pinnedRightTable": {
          "type": "string"
        },
        "editInput": {
          "type": "string"
        },
        "dragHandle": {
          "type": "string"
        },
        "footer": {
          "type": "string"
        },
        "searchBar": {
          "type": "string"
        },
        "mobileCard": {
          "type": "string"
        },
        "groupHeader": {
          "type": "string"
        },
        "resizeHandle": {
          "type": "string"
        },
        "loadingMore": {
          "type": "string"
        },
        "searchInput": {
          "type": "string"
        }
      }
    },
    "unstyled": {
      "type": "boolean",
      "description": "Removes all default styling.",
      "default": false
    },
    "style": {
      "type": "object",
      "description": "Inline styles applied to the root container."
    },
    "PinIcon": {
      "type": "object",
      "description": "React.ComponentType — Custom pin icon component (unpinned state)."
    },
    "PinnedIcon": {
      "type": "object",
      "description": "React.ComponentType — Custom pin icon component (pinned state)."
    },
    "shimmerRowCount": {
      "type": "number",
      "description": "Number of shimmer rows to display while loading.",
      "default": 10
    },
    "className": {
      "type": "string",
      "description": "CSS class for the root element."
    },
    "children": {
      "type": "object",
      "description": "React.ReactNode — Child elements."
    },
    "sortable": {
      "type": "boolean",
      "description": "Enable column sorting.",
      "default": false
    },
    "onSortingChange": {
      "type": "object",
      "description": "(sorting: SortingState) => void — Callback when sorting state changes."
    },
    "sorting": {
      "type": "array",
      "description": "Controlled sorting state."
    },
    "manualSorting": {
      "type": "boolean",
      "description": "When true, sorting is handled server-side.",
      "default": false
    },
    "selectionMode": {
      "type": "string",
      "enum": [
        "single",
        "multiple",
        "none"
      ],
      "description": "Row selection mode.",
      "default": "none"
    },
    "selectedRowIds": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Array of selected row IDs (controlled)."
    },
    "onSelectionChange": {
      "type": "object",
      "description": "(selectedIds: string[]) => void — Callback when selection changes."
    },
    "selectAllMode": {
      "type": "string",
      "enum": [
        "page",
        "all"
      ],
      "description": "Whether select-all selects current page or all rows.",
      "default": "page"
    },
    "globalFilter": {
      "type": "string",
      "description": "Global filter value for searching across all columns."
    },
    "onGlobalFilterChange": {
      "type": "object",
      "description": "(value: string) => void — Callback when global filter changes."
    },
    "columnFilters": {
      "type": "array",
      "description": "Controlled column filters state."
    },
    "onColumnFiltersChange": {
      "type": "object",
      "description": "(filters: ColumnFiltersState) => void — Callback when column filters change."
    },
    "enableColumnFilters": {
      "type": "boolean",
      "description": "Enable per-column filtering.",
      "default": false
    },
    "filterableColumns": {
      "type": "object",
      "description": "Map of column ID to filter options configuration."
    },
    "FilterIcon": {
      "type": "object",
      "description": "React.ComponentType — Custom filter icon component."
    },
    "renderColumnFilter": {
      "type": "object",
      "description": "(columnId: string, currentValues: string[], setValues: (values: string[]) => void) => ReactNode — Custom filter dropdown renderer."
    },
    "expandable": {
      "type": "boolean",
      "description": "Enable row expansion.",
      "default": false
    },
    "expandedRowIds": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Array of expanded row IDs (controlled)."
    },
    "onExpandedChange": {
      "type": "object",
      "description": "(expandedIds: string[]) => void — Callback when expanded rows change."
    },
    "renderExpandedRow": {
      "type": "object",
      "description": "(row: TData) => ReactNode — Render function for expanded row content."
    },
    "ExpandIcon": {
      "type": "object",
      "description": "React.ComponentType — Custom expand icon component."
    },
    "expandColumnPosition": {
      "type": "string",
      "enum": [
        "left",
        "right"
      ],
      "description": "Position of the expand column.",
      "default": "left"
    },
    "expandOnRowClick": {
      "type": "boolean",
      "description": "Make the entire row clickable to toggle expansion.",
      "default": false
    },
    "columnVisibility": {
      "type": "object",
      "description": "Controlled column visibility map (column ID to boolean)."
    },
    "onColumnVisibilityChange": {
      "type": "object",
      "description": "(visibility: Record<string, boolean>) => void — Callback when column visibility changes."
    },
    "striped": {
      "type": "boolean",
      "description": "Alternate row background colors.",
      "default": false
    },
    "stripedClassName": {
      "type": "string",
      "description": "Custom CSS class for striped rows."
    },
    "density": {
      "type": "string",
      "enum": [
        "compact",
        "comfortable",
        "spacious"
      ],
      "description": "Controls cell padding density.",
      "default": "comfortable"
    },
    "pinnedRightColumns": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Column IDs to pin to the right side."
    },
    "editable": {
      "type": "boolean",
      "description": "Enable inline cell editing on double-click.",
      "default": false
    },
    "editableColumns": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Column IDs that are editable."
    },
    "onCellEdit": {
      "type": "object",
      "description": "(rowId: string, columnId: string, value: unknown) => void — Callback when a cell value is edited."
    },
    "enableColumnResizing": {
      "type": "boolean",
      "description": "Enable column resizing via drag handles.",
      "default": false
    },
    "columnSizing": {
      "type": "object",
      "description": "Controlled column sizing state."
    },
    "onColumnSizingChange": {
      "type": "object",
      "description": "(sizing: ColumnSizingState) => void — Callback when column sizing changes."
    },
    "manualPagination": {
      "type": "boolean",
      "description": "When true, pagination is handled server-side.",
      "default": false
    },
    "manualFiltering": {
      "type": "boolean",
      "description": "When true, filtering is handled server-side.",
      "default": false
    },
    "columnOrder": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Controlled column order (array of column IDs)."
    },
    "onColumnOrderChange": {
      "type": "object",
      "description": "(order: string[]) => void — Callback when column order changes."
    },
    "enableRowDragDrop": {
      "type": "boolean",
      "description": "Enable row drag-and-drop reordering.",
      "default": false
    },
    "onRowReorder": {
      "type": "object",
      "description": "(fromIndex: number, toIndex: number) => void — Callback when a row is reordered."
    },
    "dragColumnPosition": {
      "type": "string",
      "enum": [
        "left",
        "right"
      ],
      "description": "Position of the drag handle column.",
      "default": "left"
    },
    "showFooter": {
      "type": "boolean",
      "description": "Show the table footer.",
      "default": false
    },
    "footerContent": {
      "type": "object",
      "description": "React.ReactNode — Custom footer content rendered inside <tfoot>."
    },
    "onContextMenu": {
      "type": "object",
      "description": "(event: React.MouseEvent, row: TData) => void — Callback on right-click of a row."
    },
    "enableCopyOnClick": {
      "type": "boolean",
      "description": "Enable single-click cell copy to clipboard.",
      "default": false
    },
    "onCellCopy": {
      "type": "object",
      "description": "(value: string, rowId: string, columnId: string) => void — Callback after a cell value is copied."
    },
    "enableInfiniteScroll": {
      "type": "boolean",
      "description": "Enable infinite scroll loading.",
      "default": false
    },
    "onLoadMore": {
      "type": "object",
      "description": "() => void — Callback when user scrolls near the bottom."
    },
    "hasMore": {
      "type": "boolean",
      "description": "Whether there is more data to load.",
      "default": false
    },
    "loadingMore": {
      "type": "boolean",
      "description": "Whether more data is currently being loaded.",
      "default": false
    },
    "infiniteEndContent": {
      "type": "object",
      "description": "React.ReactNode — Content shown when all data is loaded."
    },
    "responsiveBreakpoint": {
      "type": "number",
      "description": "Viewport width below which the table renders as card layout."
    },
    "renderMobileCard": {
      "type": "object",
      "description": "(row: TData) => ReactNode — Render function for mobile card layout."
    },
    "groupBy": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Column IDs to group rows by."
    },
    "onGroupByChange": {
      "type": "object",
      "description": "(groupBy: string[]) => void — Callback when group-by columns change."
    },
    "showSearch": {
      "type": "boolean",
      "description": "Show a built-in search bar above the table.",
      "default": false
    },
    "searchPlaceholder": {
      "type": "string",
      "description": "Placeholder text for the search bar.",
      "default": "Search..."
    },
    "searchIconPosition": {
      "type": "string",
      "enum": [
        "left",
        "right",
        "none"
      ],
      "description": "Position of the search icon.",
      "default": "left"
    },
    "CheckboxIcon": {
      "type": "object",
      "description": "React.ComponentType — Custom checkbox component for multi-row selection."
    },
    "checkboxColor": {
      "type": "string",
      "description": "Simple color customization for the default checkbox."
    },
    "DragHandleIcon": {
      "type": "object",
      "description": "React.ComponentType — Custom drag handle icon component."
    },
    "SearchIcon": {
      "type": "object",
      "description": "React.ComponentType — Custom search icon component."
    },
    "onSearchClear": {
      "type": "object",
      "description": "() => void — Callback when the search clear button is clicked."
    },
    "loadingMoreContent": {
      "type": "object",
      "description": "React.ReactNode — Custom content for the loading more indicator."
    }
  },
  "required": []
}
