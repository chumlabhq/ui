{
  "package": "@chumlab/ui",
  "version": "1.0.0",
  "generatedAt": "2026-04-28T01:04:19.074Z",
  "components": [
    {
      "name": "Accordion",
      "text": "A collapsible content panel component supporting single or multiple expanded items, keyboard navigation, and persistent state. A vertically stacked set of collapsible sections. Each item has a clickable trigger that toggles its content panel. Use Accordion for FAQs. Use type=\"multiple\" when users need several sections open. Works out-of-the-box with zero configuration beyond type and children. Notable props include type, orientation, dir, disabled, loop, classes, headingLevel, children. Accordion ships ARIA roles and keyboard support per WAI-ARIA 1.2; focus is visible and trapped where the pattern requires it. Accordion renders responsively from 320px to 1920px+ and supports both light and dark themes via the @chumlab/ui token system."
    },
    {
      "name": "Avatar",
      "text": "A user avatar component displaying an image, initials, or fallback content with optional status indicator and tooltip. A circular (or shaped) user representation showing an image, initials derived from a name, or custom fallback content. Includes group stacking, status indicators, notification badges, tooltips, and shimmer loading. Use Avatar for initials or Avatar for an image. It auto-falls back to initials if the image fails. Use AvatarGroup to stack multiple avatars with overflow counting. Works out-of-the-box with zero configuration. Notable props include name, src, alt, size, shape, maxInitials, fallback, autoColor. Avatar ships ARIA roles and keyboard support per WAI-ARIA 1.2; focus is visible and trapped where the pattern requires it. Avatar renders responsively from 320px to 1920px+ and supports both light and dark themes via the @chumlab/ui token system."
    },
    {
      "name": "Breadcrumb",
      "text": "A navigation breadcrumb component with collapsible items, custom separators, tooltips, and dropdown overflow. A navigation trail showing the user's current location in a hierarchy. Supports truncation with a dropdown for collapsed items, custom separators, icons, links, tooltips, and full style customization. Pass an items array of { id, label } objects. The last item is treated as the active page. Use maxVisibleItems to collapse long trails into a dropdown. Works out-of-the-box with zero configuration beyond items. Notable props include items, maxVisibleItems, separator, onItemClick, aria-label, classes, unstyled, className. Breadcrumb ships ARIA roles and keyboard support per WAI-ARIA 1.2; focus is visible and trapped where the pattern requires it. Breadcrumb renders responsively from 320px to 1920px+ and supports both light and dark themes via the @chumlab/ui token system."
    },
    {
      "name": "Button",
      "text": "A polymorphic button component that renders as button, anchor, span, or merges into a child element via asChild. A polymorphic, accessible button that renders as , , or . Provides structural defaults (padding, focus ring, disabled state) — visual theme is applied via className. Supports icons with animations, loading states, tooltips, and grouping. ButtonLabel — the component provides structure (padding, rounded corners, focus ring, disabled handling). You pass visual styles (colors, shadows) via className. Use as=\"a\" for links, startIcon/endIcon for icons, loading for async states. Group buttons with ButtonGroup. Notable props include children, as, asChild, size, disabled, startIcon, endIcon, loading. Button ships ARIA roles and keyboard support per WAI-ARIA 1.2; focus is visible and trapped where the pattern requires it. Button renders responsively from 320px to 1920px+ and supports both light and dark themes via the @chumlab/ui token system."
    },
    {
      "name": "CascadingDropdown",
      "text": "A multi-level cascading dropdown supporting single and multi-select modes, async child loading, and search filtering. A multi-level dropdown menu with nested submenus. Supports single and multi-select per level, async data loading, and search with debounce. Use CascadingDropdown for hierarchical data (categories → subcategories). Children can use selectionMode: \"single\" or \"multi\" independently. For async data, pass onLoadChildren to fetch children on hover. For search, set showMenuSearch and/or showSubmenuSearch. Notable props include options, value, defaultValue, onValueChange, onLoadChildren, onLoadError, open, defaultOpen. CascadingDropdown ships ARIA roles and keyboard support per WAI-ARIA 1.2; focus is visible and trapped where the pattern requires it. CascadingDropdown renders responsively from 320px to 1920px+ and supports both light and dark themes via the @chumlab/ui token system."
    },
    {
      "name": "Checkbox",
      "text": "A checkbox input component with label, description, indeterminate state, and validation support. A customizable checkbox with label, description, indeterminate state, custom icons, sizes, shapes, and accessible via native . Use Checkbox for a basic labeled checkbox. Supports indeterminate for tree/list \"select all\" patterns. Works out-of-the-box with built-in dark mode styles. Notable props include label, description, checked, defaultChecked, indeterminate, onValueChange, onCheckedChange, onFocus. Checkbox ships ARIA roles and keyboard support per WAI-ARIA 1.2; focus is visible and trapped where the pattern requires it. Checkbox renders responsively from 320px to 1920px+ and supports both light and dark themes via the @chumlab/ui token system."
    },
    {
      "name": "CountryFlag",
      "text": "A country flag component displaying flag images by ISO country code with optional tooltip and loading states. Renders a country flag image from a two-letter ISO country code. Supports sizes, tooltips, grouping, shimmer loading, and error fallback. Use CountryFlag to render a flag. Pass size for preset or pixel size. Use CountryFlagGroup to show a row of flags with overflow count. Flags load from /flags/{code}.svg by default. Notable props include code, size, aspectRatio, alt, fallback, loading, tooltip, basePath. CountryFlag ships ARIA roles and keyboard support per WAI-ARIA 1.2; focus is visible and trapped where the pattern requires it. CountryFlag renders responsively from 320px to 1920px+ and supports both light and dark themes via the @chumlab/ui token system."
    },
    {
      "name": "DatePicker",
      "text": "A date picker component supporting single, range, and multiple date selection with calendar navigation, presets, and markers. A full-featured date picker supporting single, range, and multiple date selection with calendar popover, presets, markers, locale support, and keyboard navigation. Use DatePicker for single date selection (default). Use mode=\"range\" for date ranges and mode=\"multiple\" for selecting several individual dates. Works out-of-the-box with built-in styles and sensible defaults. Notable props include mode, value, onValueChange, onClear, onMonthChange, open, defaultOpen, onOpenChange. DatePicker ships ARIA roles and keyboard support per WAI-ARIA 1.2; focus is visible and trapped where the pattern requires it. DatePicker renders responsively from 320px to 1920px+ and supports both light and dark themes via the @chumlab/ui token system."
    },
    {
      "name": "Drawer",
      "text": "A slide-out panel component supporting all four directions, swipe gestures, snap points, and focus trapping. A sliding panel overlay with focus trapping, scroll lock, swipe-to-close, snap points, stacked drawer support, and keyboard navigation. Use Drawer with DrawerHeader, DrawerBody, DrawerFooter, DrawerCloseButton as children. Supports all 4 directions, focus trapping, scroll lock, swipe-to-close, and snap points. Notable props include open, defaultOpen, onOpenChange, zIndex, children, direction, size, overlayColor. Drawer ships ARIA roles and keyboard support per WAI-ARIA 1.2; focus is visible and trapped where the pattern requires it. Drawer renders responsively from 320px to 1920px+ and supports both light and dark themes via the @chumlab/ui token system."
    },
    {
      "name": "Dropdown",
      "text": "A single-select dropdown with keyboard navigation, async loading, custom triggers, and portal support. A single-select dropdown (combobox) with portal rendering, keyboard navigation, async loading, and full style customization. Use Dropdown for a styled select control. Supports controlled (value + onValueChange) and uncontrolled (defaultValue) modes. Renders the popup via a React Portal so it's never clipped by overflow: hidden ancestors. Works out-of-the-box with built-in Tailwind styles. Notable props include options, value, defaultValue, onValueChange, open, defaultOpen, onOpenChange, id. Dropdown ships ARIA roles and keyboard support per WAI-ARIA 1.2; focus is visible and trapped where the pattern requires it. Dropdown renders responsively from 320px to 1920px+ and supports both light and dark themes via the @chumlab/ui token system."
    },
    {
      "name": "Input",
      "text": "A text input field with label, prefix/suffix, icons, clearable, character count, loading state, and validation support. A production-grade text input with labels, icons, prefix/suffix, clearable, character count, validation states, and full form integration. Use Input for a labeled input. Built-in styles include dark mode, focus ring, error/success states. Use onValueChange for simple string callback or onChange for the DOM event. Notable props include label, description, error, errorMessage, success, successMessage, size, prefix. Input ships ARIA roles and keyboard support per WAI-ARIA 1.2; focus is visible and trapped where the pattern requires it. Input renders responsively from 320px to 1920px+ and supports both light and dark themes via the @chumlab/ui token system."
    },
    {
      "name": "InternationalPhoneInput",
      "text": "A phone number input with country code selector, validation, formatting, paste detection, and copy format support. Use InternationalPhoneInput for phone number entry with country selection. Supports controlled (value + onValueChange) and uncontrolled (defaultValue) modes. Auto-formats numbers by country, validates on blur, detects country from pasted international numbers, and copies in E.164/international/national format. Works out-of-the-box with built-in Tailwind styles. Notable props include value, defaultValue, onValueChange, onCountryChange, defaultCountry, countries, preferredCountries, id. InternationalPhoneInput ships ARIA roles and keyboard support per WAI-ARIA 1.2; focus is visible and trapped where the pattern requires it. InternationalPhoneInput renders responsively from 320px to 1920px+ and supports both light and dark themes via the @chumlab/ui token system."
    },
    {
      "name": "Loader",
      "text": "Loading indicator components in four variants: CircularLoader, LinearLoader, DotLoader, and PulseLoader. Animated loading indicators. Four variants: CircularLoader, LinearLoader, DotLoader, PulseLoader. Use CircularLoader for a spinning indicator, LinearLoader for a progress bar, DotLoader for bouncing dots, PulseLoader for a pulsing ring. All work out-of-the-box with role=\"status\" and aria-label. Loader ships ARIA roles and keyboard support per WAI-ARIA 1.2; focus is visible and trapped where the pattern requires it. Loader renders responsively from 320px to 1920px+ and supports both light and dark themes via the @chumlab/ui token system."
    },
    {
      "name": "Modal",
      "text": "An accessible modal dialog with focus trapping, scroll locking, animations, nesting support, and compound sub-components. Accessible dialog overlay with focus trapping, scroll lock, nested modals, animations, and full keyboard navigation. Use Modalcontent for a dialog overlay. Built-in overlay with blur, focus trapping, Escape to close, scroll lock, and dark mode. Supports nested modals, custom sizing, fullscreen mode, and the useModal() hook for child access. Works out-of-the-box with built-in Tailwind styles. Notable props include open, onOpenChange, defaultOpen, children, title, description, icon, showIcon. Modal ships ARIA roles and keyboard support per WAI-ARIA 1.2; focus is visible and trapped where the pattern requires it. Modal renders responsively from 320px to 1920px+ and supports both light and dark themes via the @chumlab/ui token system."
    },
    {
      "name": "MultiSelectDropdown",
      "text": "A multi-select dropdown with chip display, keyboard navigation, async loading, and custom triggers. Use MultiSelectDropdown for a multi-select control with chip display. Supports controlled/uncontrolled modes, async option loading, custom rendering, and portal-based positioning. Works out-of-the-box with built-in Tailwind styles. Notable props include options, value, defaultValue, onValueChange, id, name, placeholder, disabled. MultiSelectDropdown ships ARIA roles and keyboard support per WAI-ARIA 1.2; focus is visible and trapped where the pattern requires it. MultiSelectDropdown renders responsively from 320px to 1920px+ and supports both light and dark themes via the @chumlab/ui token system."
    },
    {
      "name": "MultiSelectSearchableDropdown",
      "text": "A multi-select dropdown with search filtering, chip display, async loading, and keyboard navigation. A multi-select dropdown with built-in search, chip display, async search/prefetch, checkbox options, and full style customization. Use MultiSelectSearchableDropdown for a searchable multi-select control with chip display. Supports local filtering, async search via onSearch, initial prefetch via onLoadInitialOptions, and portal-based positioning. Works out-of-the-box with built-in Tailwind styles. Notable props include options, value, defaultValue, onValueChange, id, name, placeholder, disabled. MultiSelectSearchableDropdown ships ARIA roles and keyboard support per WAI-ARIA 1.2; focus is visible and trapped where the pattern requires it. MultiSelectSearchableDropdown renders responsively from 320px to 1920px+ and supports both light and dark themes via the @chumlab/ui token system."
    },
    {
      "name": "OtpInput",
      "text": "A one-time password input with configurable length, grouping, paste support, and custom input rendering. A one-time password input with per-character fields, keyboard navigation, paste support, grouping, and validation. Use OtpInput for a 6-digit OTP input. Supports length, type (numeric/alphanumeric), masked mode, groupPattern for visual grouping, and built-in error/success states. Notable props include length, value, defaultValue, onValueChange, onComplete, label, id, name. OtpInput ships ARIA roles and keyboard support per WAI-ARIA 1.2; focus is visible and trapped where the pattern requires it. OtpInput renders responsively from 320px to 1920px+ and supports both light and dark themes via the @chumlab/ui token system."
    },
    {
      "name": "Pagination",
      "text": "A pagination control with page buttons, rows-per-page selector, ellipsis, and customizable navigation icons. Accessible page navigation with page buttons, rows-per-page selector, custom ellipsis, page info display, section reordering, i18n, and full style customization. Use Pagination for basic pagination. Add showRowsPerPage for a rows-per-page dropdown. Supports custom ellipsis rendering, page info display, section reordering, i18n labels, and full class-driven styling. Works out-of-the-box with built-in Tailwind styles and dark mode. Notable props include totalPages, siblingCount, rowsPerPage, rowOptions, disabled, onValueChange, value, defaultValue. Pagination ships ARIA roles and keyboard support per WAI-ARIA 1.2; focus is visible and trapped where the pattern requires it. Pagination renders responsively from 320px to 1920px+ and supports both light and dark themes via the @chumlab/ui token system."
    },
    {
      "name": "RadioButton",
      "text": "A radio button component with RadioButton and RadioGroup for single-selection within a group. A radio button group with labels, descriptions, custom icons, sizes, orientations, and validation states. Use RadioGroupRadioButton... for a radio group. Uses native — full keyboard support (arrow keys) out-of-the-box. Notable props include RadioButton, RadioGroup. RadioButton ships ARIA roles and keyboard support per WAI-ARIA 1.2; focus is visible and trapped where the pattern requires it. RadioButton renders responsively from 320px to 1920px+ and supports both light and dark themes via the @chumlab/ui token system."
    },
    {
      "name": "ResizablePanel",
      "text": "A panel component with a draggable resize handle that supports horizontal and vertical resizing. A draggable resizable panel with constraints, keyboard resizing, Escape to cancel, and WAI-ARIA separator semantics. Use ResizablePanelcontent. Drag the edge to resize. Supports controlled/uncontrolled, all 4 directions, keyboard arrow keys, Escape to cancel. Notable props include value, defaultValue, onValueChange, onResizeStart, onResizeEnd, minValue, maxValue, resizeDirection. ResizablePanel ships ARIA roles and keyboard support per WAI-ARIA 1.2; focus is visible and trapped where the pattern requires it. ResizablePanel renders responsively from 320px to 1920px+ and supports both light and dark themes via the @chumlab/ui token system."
    },
    {
      "name": "SearchableDropdown",
      "text": "A dropdown component with built-in search filtering, async loading, and keyboard navigation. A single-select dropdown with built-in search input, async search/prefetch, portal rendering, keyboard navigation, and full style customization. Use SearchableDropdown for a searchable single-select. Supports local filtering by default, async search via onSearch, initial prefetch via onLoadInitialOptions, and portal-based positioning. Controlled (value + onValueChange) or uncontrolled (defaultValue). Works out-of-the-box with built-in Tailwind styles. Notable props include options, value, defaultValue, onValueChange, open, defaultOpen, onOpenChange, id. SearchableDropdown ships ARIA roles and keyboard support per WAI-ARIA 1.2; focus is visible and trapped where the pattern requires it. SearchableDropdown renders responsively from 320px to 1920px+ and supports both light and dark themes via the @chumlab/ui token system."
    },
    {
      "name": "Slider",
      "text": "A range slider component supporting single and dual-thumb modes with marks, tooltips, and form integration. A range slider with single and dual-thumb modes, marks, tooltips, vertical orientation, and full keyboard navigation. Use Slider for a single slider (0-100). Pass value={[20, 80]} for a range slider. Supports marks, showTooltip, orientation=\"vertical\", and step. Notable props include value, defaultValue, onValueChange, onValueCommit, min, max, step, minStepsBetweenThumbs. Slider ships ARIA roles and keyboard support per WAI-ARIA 1.2; focus is visible and trapped where the pattern requires it. Slider renders responsively from 320px to 1920px+ and supports both light and dark themes via the @chumlab/ui token system."
    },
    {
      "name": "Stepper",
      "text": "A multi-step progress indicator with numbered, icon, or dot variants and horizontal/vertical orientation. A step progress indicator with numbered/icon/dot variants, horizontal/vertical layouts, clickable steps, tooltips, and keyboard navigation. Use Stepper for a controlled stepper. Steps have id, label, optional description and icon. Supports horizontal/vertical layouts, numbered/icon/dot variants, tooltips, non-linear navigation, and responsive sizing. Works out-of-the-box with built-in Tailwind styles. Notable props include steps, value, defaultValue, onValueChange, beforeStepChange, orientation, variant, activationMode. Stepper ships ARIA roles and keyboard support per WAI-ARIA 1.2; focus is visible and trapped where the pattern requires it. Stepper renders responsively from 320px to 1920px+ and supports both light and dark themes via the @chumlab/ui token system."
    },
    {
      "name": "Switch",
      "text": "A toggle switch component with label, description, loading state, and custom icons. A toggle switch for binary on/off states with labels, validation, loading, custom icons, and full WAI-ARIA support. Use Switch for a labeled toggle. Uses native with aria-checked. Supports error/success states, loading, custom icons on thumb, and CSS custom properties for theming. Notable props include className, label, description, checked, defaultChecked, onValueChange, onCheckedChange, name. Switch ships ARIA roles and keyboard support per WAI-ARIA 1.2; focus is visible and trapped where the pattern requires it. Switch renders responsively from 320px to 1920px+ and supports both light and dark themes via the @chumlab/ui token system."
    },
    {
      "name": "Table",
      "text": "A feature-rich data table with sorting, filtering, selection, expansion, pinning, inline editing, infinite scroll, and responsive mobile card layout. Use Table for a data table. Built on TanStack Table with built-in sorting, row selection, column pinning, row expansion, drag-and-drop reordering, inline editing, column resizing, infinite scroll, search, and CSV export. Pair with Pagination for paged data. Works out-of-the-box with built-in Tailwind styles. Notable props include columns, data, COLUMNS, COLUMNS_DATA, loading, showHeader, tableHeader, pinnedColumns. Table ships ARIA roles and keyboard support per WAI-ARIA 1.2; focus is visible and trapped where the pattern requires it. Table renders responsively from 320px to 1920px+ and supports both light and dark themes via the @chumlab/ui token system."
    },
    {
      "name": "TabPanel",
      "text": "A tabbed interface component with keyboard navigation, icons, counts, tooltips, and horizontal/vertical orientation. Accessible tab panel with icons, badges, tooltips, closable tabs, vertical orientation, lazy loading, and roving tabindex. Use TabPanel ...} /> for a tabbed interface. Supports controlled/uncontrolled, icons, count badges, closable tabs, vertical orientation, manual activation, tooltips, and responsive horizontal scrolling. Works out-of-the-box with built-in Tailwind styles. Notable props include tabs, id, value, defaultValue, onValueChange, children, orientation, activationMode. TabPanel ships ARIA roles and keyboard support per WAI-ARIA 1.2; focus is visible and trapped where the pattern requires it. TabPanel renders responsively from 320px to 1920px+ and supports both light and dark themes via the @chumlab/ui token system."
    },
    {
      "name": "TextArea",
      "text": "A multi-line text input with label, validation states, character count, auto-resize, clearable, and icon support. A production-grade multi-line text input with auto-resize, character count, validation states, icons, and full form integration. Use TextArea for a labeled textarea. Supports autoResize to grow with content, showCount + maxLength for character limits, and built-in dark mode. Notable props include label, description, error, errorMessage, success, successMessage, size, startIcon. TextArea ships ARIA roles and keyboard support per WAI-ARIA 1.2; focus is visible and trapped where the pattern requires it. TextArea renders responsively from 320px to 1920px+ and supports both light and dark themes via the @chumlab/ui token system."
    },
    {
      "name": "TimePicker",
      "text": "A time selection component with dropdown list or analog clock face variant, supporting 12h/24h formats and min/max time constraints. A time input with dropdown list or analog clock variant, smart parsing, 12/24-hour formats, min/max constraints, clearable, keyboard navigation, and full accessibility. Use TimePicker for time selection. Supports dropdown (list) and clock (analog) variants, 12h/24h formats, min/max constraints, smart parsing, clearable, and portal-based positioning. Works out-of-the-box with built-in Tailwind styles. Notable props include value, defaultValue, onValueChange, onBlur, format, minuteStep, minTime, maxTime. TimePicker ships ARIA roles and keyboard support per WAI-ARIA 1.2; focus is visible and trapped where the pattern requires it. TimePicker renders responsively from 320px to 1920px+ and supports both light and dark themes via the @chumlab/ui token system."
    },
    {
      "name": "Toast",
      "text": "A notification toast component with success, warning, error, and info types, progress bar, and configurable position. A notification system for displaying brief messages. Supports multiple variants, positions, progress bars, custom content, and auto-dismiss. Wrap your app with ToastProvider, then call toast.success(\"Done!\") from any child via useToast(). Supports 4 types (success/warning/error/info), 6 positions, auto-dismiss with duration, progress bars, and custom content. Notable props include id, type, message, description, content, duration, showProgress, progressColor. Toast ships ARIA roles and keyboard support per WAI-ARIA 1.2; focus is visible and trapped where the pattern requires it. Toast renders responsively from 320px to 1920px+ and supports both light and dark themes via the @chumlab/ui token system."
    },
    {
      "name": "Tooltip",
      "text": "A tooltip component with configurable positioning, arrow, shadow, truncation, and portal support. Accessible tooltip with configurable positioning, delays, rich content, arrows, shadows, truncation, and full dark mode support. Use TooltipHover. Supports side (top/right/bottom/left), align, delays, arrows, rich HTML content, controlled open state, and auto-truncation with tooltip on overflow. Notable props include children, content, side, align, sideOffset, alignOffset, maxWidth, wordWrap. Tooltip ships ARIA roles and keyboard support per WAI-ARIA 1.2; focus is visible and trapped where the pattern requires it. Tooltip renders responsively from 320px to 1920px+ and supports both light and dark themes via the @chumlab/ui token system."
    }
  ]
}
