# @chumlab/ui — Full Component Reference

> Production-grade React component library. TypeScript, Tailwind CSS v4, WCAG 2.1 AA.
> This file is optimized for AI coding tools. For human docs, visit https://chumlab.com

Package: @chumlab/ui | React 18+/19 | MIT License
GitHub: https://github.com/chumlabhq/ui

## Install
npm install @chumlab/ui

## Tailwind CSS setup (required)
In your CSS file where you import Tailwind, add:
@source "../node_modules/@chumlab/ui/dist/**/*.js";

## Import patterns
import { Button } from "@chumlab/ui";           // full library
import { Button } from "@chumlab/ui/button";     // per-component

## Shared patterns
- Controlled: value + onValueChange. Uncontrolled: defaultValue. Never combine both.
- Styling: classes prop overrides per-slot. unstyled strips all defaults. className for root.
- Dark mode: Tailwind dark: variants. Add .dark class to <html>.
- All components are WCAG 2.1 AA, keyboard navigable, screen reader compatible.

---

## Accordion

> A vertically stacked set of collapsible sections. Each item has a clickable trigger that toggles its content panel.

### Import

```tsx
import {
  Accordion, AccordionItem, AccordionTrigger, AccordionContent
} from "@chumlab/ui/accordion";
```

### Basic Usage

```tsx
<Accordion type="single" collapsible defaultValue="q1">
  <AccordionItem value="q1">
    <AccordionTrigger>What is your return policy?</AccordionTrigger>
    <AccordionContent>You can return any item within 30 days.</AccordionContent>
  </AccordionItem>
  <AccordionItem value="q2">
    <AccordionTrigger>How long does shipping take?</AccordionTrigger>
    <AccordionContent>Standard shipping takes 3-5 business days.</AccordionContent>
  </AccordionItem>
</Accordion>
```

### Accordion (root) Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `type` | `"single" \| "multiple"` | **required** | Single = one open at a time. Multiple = any number. |
| `collapsible` | `boolean` | `false` | (single only) Allow closing the open item. |
| `value` | `string \| string[]` | — | Controlled expanded value(s). |
| `defaultValue` | `string \| string[]` | — | Uncontrolled initial value(s). |
| `onValueChange` | `(value) => void` | — | Callback when expanded items change. |
| `size` | `"sm" \| "md" \| "lg"` | `"md"` | Padding and font size preset. |
| `variant` | `"default" \| "bordered" \| "separated" \| "flush"` | `"default"` | Visual style. |
| `disabled` | `boolean` | `false` | Disable all items globally. |
| `orientation` | `"vertical" \| "horizontal"` | `"vertical"` | Arrow key navigation direction. |
| `dir` | `"ltr" \| "rtl"` | `"ltr"` | Text direction. |
| `loop` | `boolean` | `true` | Wrap keyboard focus from last to first. |
| `headingLevel` | `1-6` | `3` | HTML heading level wrapping triggers. |
| `animationDuration` | `number` | `300` | Expand/collapse animation in ms. |
| `animationEasing` | `string` | `"ease-in-out"` | CSS easing function. |
| `reduceMotion` | `boolean \| "auto"` | `"auto"` | "auto" respects OS setting. |
| `unstyled` | `boolean` | `false` | Strip all default classes. |
| `classes` | `AccordionClasses` | — | Override classes per slot. |
| `maxExpanded` | `number` | — | (multiple only) Max simultaneously open items. |
| `defaultExpandAll` | `boolean` | `false` | (multiple, uncontrolled only) Open all on mount. |
| `storageKey` | `string \| StorageConfig` | — | Persist state to localStorage (uncontrolled only). |
| `expandOnPrint` | `boolean` | `false` | Force-expand all when printing. |
| `announceExpanded` | `boolean` | `false` | aria-live announcements on toggle. |
| `preventClose` | `(value: string) => boolean \| Promise<boolean>` | — | Conditionally prevent closing. |
| `onExpandedChange` | `(event) => void` | — | Detailed expand/collapse event callback. |
| `asChild` | `boolean` | `false` | Render as child element (polymorphic). |

### AccordionItem Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `value` | `string` | **required** | Unique identifier. |
| `disabled` | `boolean` | `false` | Disable this item. |
| `onExpandedChange` | `(isExpanded: boolean) => void` | — | Per-item toggle callback. |

### AccordionTrigger Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `iconPosition` | `"left" \| "right" \| "none"` | `"right"` | Where the chevron icon appears. |
| `iconAnimation` | `"rotate" \| "switch" \| "none"` | `"rotate"` | How the icon animates. |
| `expandedIcon` | `ReactNode` | — | Custom icon when expanded. |
| `collapsedIcon` | `ReactNode` | — | Custom icon when collapsed. |
| `subtitle` | `ReactNode` | — | Secondary text below the label. |
| `leftSlot` | `ReactNode` | — | Content to the left of the label. |
| `rightSlot` | `ReactNode` | — | Content to the right (before icon). |

### AccordionContent Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `forceMount` | `boolean` | `false` | Always render in DOM. |
| `lazyLoad` | `boolean` | `false` | Don't render until first opened. |
| `unmountOnClose` | `boolean` | `false` | Remove from DOM when closed. |
| `onOpenStart` | `() => void` | — | Fires when expand animation begins. |
| `onOpenEnd` | `() => void` | — | Fires when expand animation completes. |
| `onCloseStart` | `() => void` | — | Fires when collapse animation begins. |
| `onCloseEnd` | `() => void` | — | Fires when collapse animation completes. |

### AccordionShimmer Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `count` | `number` | `3` | Number of skeleton items. |
| `size` | `"sm" \| "md" \| "lg"` | `"md"` | Matches Accordion size. |
| `variant` | `"default" \| "bordered" \| "separated" \| "flush"` | `"default"` | Matches Accordion variant. |
| `animate` | `boolean` | `true` | Pulse animation. |
| `showContent` | `boolean` | `false` | Show content placeholder lines. |

### Prop Constraints

- `collapsible` only works with `type="single"`. Ignored on `type="multiple"`.
- `maxExpanded` only works with `type="multiple"`. Ignored on `type="single"`.
- `defaultExpandAll` only works with `type="multiple"` AND uncontrolled mode.
- `storageKey` only works in uncontrolled mode (no `value` prop).
- Never combine `value` + `defaultValue`. Use one or the other.
- `value` requires `onValueChange` — controlled mode needs both.

### Class Slots

```
root
├── item
│   ├── heading
│   │   └── trigger
│   │       ├── triggerLeft
│   │       ├── triggerInner
│   │       │   └── subtitle
│   │       ├── triggerRight
│   │       └── iconWrapper > icon
│   └── contentWrapper (animation layer)
│       └── contentInner
│           └── content
```

### Data Attributes

- `data-state="open|closed"` — on item, trigger, and content
- `data-disabled` — on disabled items/triggers
- `data-pending` — during async preventClose
- `data-orientation="vertical|horizontal"` — on root
- `data-value` — item's unique value string
- `data-type="single|multiple"`, `data-size`, `data-variant` — on root

### Ref API (Imperative)

```tsx
const ref = useRef<AccordionRef>(null);

<Accordion ref={ref} type="multiple">...</Accordion>

// Available methods:
ref.current.expandAll();
ref.current.collapseAll();
ref.current.expand("item-1");
ref.current.collapse("item-1");
ref.current.toggle("item-1");
ref.current.focusItem("item-1");
ref.current.getExpandedValues();   // Set<string>
ref.current.isExpanded("item-1");  // boolean
ref.current.getItemCount();        // number
```

### Advanced Pattern: Controlled accordion

```tsx
const [openItem, setOpenItem] = useState<string | null>("settings");

<Accordion type="single" collapsible value={openItem} onValueChange={setOpenItem}>
  <AccordionItem value="settings">
    <AccordionTrigger>Settings</AccordionTrigger>
    <AccordionContent>...</AccordionContent>
  </AccordionItem>
  <AccordionItem value="billing">
    <AccordionTrigger>Billing</AccordionTrigger>
    <AccordionContent>...</AccordionContent>
  </AccordionItem>
</Accordion>
```

---

## Avatar

> A circular (or shaped) user representation showing an image, initials derived from a name, or custom fallback content. Includes group stacking, status indicators, notification badges, tooltips, and shimmer loading.

### Import

```tsx
import { Avatar, AvatarGroup, AvatarGroupCount, AvatarBadge, AvatarShimmer, AvatarGroupShimmer } from "@chumlab/ui/avatar";
```

### Basic Usage

```tsx
<Avatar src="https://example.com/photo.jpg" alt="John Doe" name="John Doe" />
<Avatar name="Jane Smith" autoColor />
<AvatarGroup max={3}>
  <Avatar name="Alice" autoColor />
  <Avatar name="Bob" autoColor />
  <Avatar name="Charlie" autoColor />
  <Avatar name="Diana" autoColor />
</AvatarGroup>
```

### Avatar Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `name` | `string` | — | Display name, used for initials and autoColor hash. |
| `src` | `string` | — | Image URL. Falls back to initials on error. |
| `alt` | `string` | — | Image alt text. |
| `size` | `"xs" \| "sm" \| "md" \| "lg" \| "xl" \| number` | `"md"` | Preset or custom pixel size. |
| `shape` | `"circle" \| "square" \| "rounded"` | `"circle"` | Visual shape. |
| `maxInitials` | `number` | `2` | Max initials to display. |
| `fallback` | `ReactNode` | — | Custom fallback content (icon, emoji, etc.). |
| `autoColor` | `boolean` | `false` | Deterministic bg/text color from name hash. |
| `colors` | `AvatarColors` | — | Custom color palettes for autoColor. |
| `bordered` | `boolean \| string` | — | Border ring. true = auto color, string = CSS value. |
| `status` | `AvatarStatus \| AvatarStatusConfig` | — | Presence indicator (online/offline/away/busy). |
| `tooltip` | `ReactNode \| AvatarTooltipConfig` | — | Hover tooltip. |
| `imageConfig` | `AvatarImageConfig` | — | Advanced img attrs (srcSet, loading, crossOrigin). |
| `classes` | `AvatarClasses` | — | Per-slot class overrides. |
| `unstyled` | `boolean` | `false` | Strip all default classes. |
| `textStyle` | `CSSProperties` | — | Inline styles for initials text. |
| `loading` | `boolean` | `false` | Show shimmer placeholder instead of content. |
| `reduceMotion` | `boolean \| "auto"` | `"auto"` | Disable image fade-in animation. |
| `onLoad` | `() => void` | — | Image load success callback. |
| `onError` | `() => void` | — | Image load error callback. |

### AvatarGroup Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `max` | `number` | — | Max visible avatars before surplus count. |
| `size` | `AvatarSize` | `"md"` | Inherited by all children via context. |
| `shape` | `AvatarShape` | `"circle"` | Inherited by all children. |
| `bordered` | `boolean \| string` | — | Inherited by all children. |
| `spacing` | `number` | `-8` | Overlap in px (negative = overlap). |
| `ringColor` | `string` | `"white"` | Ring color between stacked avatars. |
| `showTooltip` | `boolean` | `false` | Show hidden names as tooltip on surplus. |
| `total` | `number` | — | Override total count for surplus calculation. |
| `variant` | `"stack" \| "grid" \| "inline"` | `"stack"` | Layout mode. |
| `reverseOrder` | `boolean` | `false` | Reverse stacking z-order. |
| `renderSurplus` | `(count: number) => ReactNode` | — | Custom surplus renderer. |
| `onAvatarClick` | `(info, event) => void` | — | Click handler with { index, name }. |
| `dir` | `"ltr" \| "rtl"` | `"ltr"` | Text direction. |
| `classes` | `AvatarGroupClasses` | — | Per-slot: root, item, surplus. |

### AvatarBadge Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `count` | `number` | — | Notification count. |
| `max` | `number` | `99` | Shows 99+ when exceeded. |
| `showZero` | `boolean` | `false` | Show badge when count is 0. |
| `dot` | `boolean` | `false` | Small dot instead of count. |
| `position` | `CornerPosition` | `"top-right"` | Corner placement. |
| `offset` | `BadgeOffset` | — | Pixel offset { x, y }. |
| `color` | `string` | — | Background color override. |
| `size` | `"xs" \| "sm" \| "md" \| "lg"` | `"md"` | Size preset. |
| `variant` | `"solid" \| "outline" \| "soft"` | `"solid"` | Visual style. |
| `pulse` | `boolean` | `false` | Pulsing animation. |
| `invisible` | `boolean` | `false` | Hide without unmounting. |

### AvatarGroupCount Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `count` | `number` | **required** | Number to display. |
| `showPlus` | `boolean` | `true` | Show + prefix. |
| `format` | `(count: number) => string` | — | Custom formatter. |
| `variant` | `"solid" \| "outline" \| "ghost"` | `"solid"` | Visual style. |

### Prop Constraints

- `autoColor` requires `name` to generate colors. Without `name`, has no effect.
- `colors` only used when `autoColor={true}`.
- `bordered={true}` uses auto-generated border color if `autoColor` is on.
- AvatarGroup `ringColor` — match to your background color for clean overlap.
- AvatarGroup `variant` — only "stack" uses `spacing` and `ringColor`.

### Class Slots

```
root (outer container, sets size/shape)
├── inner (absolute inset, overflow hidden)
│   ├── image
│   ├── initials
│   └── fallback
└── status (positioned absolute)
```

### Data Attributes

- `data-has-image` — on Avatar root when image is loaded (boolean)
- `data-shape="circle|rounded|square"` — on Avatar root

### Advanced Pattern: Badge notification on avatar

```tsx
<div className="relative inline-block">
  <Avatar name="John" src="/john.jpg" />
  <AvatarBadge count={5} position="top-right" variant="solid" pulse />
</div>
```

---

## Breadcrumb

> A navigation trail showing the user's current location in a hierarchy. Supports truncation with a dropdown for collapsed items.

### Import

```tsx
import { Breadcrumb } from "@chumlab/ui/breadcrumb";
import type { BreadcrumbItem } from "@chumlab/ui/breadcrumb";
```

### Basic Usage

```tsx
<Breadcrumb
  items={[
    { id: "home", label: "Home" },
    { id: "products", label: "Products" },
    { id: "phones", label: "Phones" },
  ]}
  onItemClick={(item) => console.log("Navigate to:", item.label)}
/>
```

### Breadcrumb Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `items` | `BreadcrumbItem[]` | **required** | Array of breadcrumb items. |
| `maxVisibleItems` | `number` | `4` | Max before truncation. |
| `separator` | `ReactNode` | — | Custom separator content. |
| `separatorIcon` | `ComponentType` | `ChevronRightIcon` | Separator component. |
| `onItemClick` | `(item: BreadcrumbItem) => void` | — | Global click handler. |
| `classes` | `BreadcrumbClasses` | — | Per-slot class overrides. |
| `unstyled` | `boolean` | `false` | Strip all default classes. |
| `iconSize` | `number \| string` | — | Size for separator and ellipsis icons. |
| `showTooltips` | `boolean` | `true` | Enable tooltips globally. |
| `tooltipPosition` | `TooltipSide` | — | Default tooltip side. |
| `tooltipOffset` | `number` | — | Tooltip distance offset. |
| `defaultTooltipProps` | `BreadcrumbTooltipProps` | — | Default tooltip config. |
| `ellipsisTooltip` | `ReactNode` | — | Tooltip for the ellipsis button. |
| `ellipsisAriaLabel` | `string` | `"Show collapsed breadcrumb items"` | Aria label for ellipsis. |
| `dropdownPosition` | `"top" \| "bottom" \| "left" \| "right"` | `"top"` | Collapsed dropdown direction. |
| `dropdownZIndex` | `number` | `50` | Dropdown z-index. |
| `portalContainer` | `HTMLElement \| null` | `document.body` | Portal target. |
| `onDropdownOpenChange` | `(open: boolean) => void` | — | Dropdown open/close callback. |
| `aria-label` | `string` | `"Breadcrumb"` | Nav landmark label. |

### BreadcrumbItem (data shape)

| Property | Type | Default | Description |
|----------|------|---------|-------------|
| `id` | `string` | **required** | Unique identifier. |
| `label` | `string` | **required** | Display text. |
| `href` | `string` | — | Renders as `<a>` link. |
| `onClick` | `() => void` | — | Per-item click (overrides global). |
| `icon` | `ReactNode` | — | Icon element. |
| `iconPosition` | `"left" \| "right"` | `"left"` | Icon placement. |
| `disabled` | `boolean` | `false` | Disables interaction. |
| `content` | `ReactNode` | — | Custom content instead of label text. |
| `tooltip` | `ReactNode` | — | Tooltip content. |
| `tooltipProps` | `BreadcrumbTooltipProps` | — | Per-item tooltip config. |

### Prop Constraints

- `items` must have at least one item. Last item is always active.
- `separator` takes priority over `separatorIcon`.
- `href` renders item as `<a>`. Disabled items always render as `<button>`.
- `item.onClick` overrides global `onItemClick`.

### Class Slots

```
root (nav)
└── list (ol)
    ├── item / itemActive / itemDisabled / link
    ├── separator
    ├── ellipsisButton
    ├── icon
    └── dropdown (portal)
        └── dropdownItem
```

### Data Attributes

- `data-truncated="true"` — on root when items are truncated
- `data-dropdown-open="true"` — when ellipsis dropdown is open
- `data-state="active|inactive"` — on each item
- `data-disabled="true"` — on disabled items
- `aria-current="page"` — on the last (active) item

### Advanced Pattern: Truncated trail with router

```tsx
<Breadcrumb
  items={deepPath}
  maxVisibleItems={3}
  onItemClick={(item) => router.push(item.href ?? "/")}
/>
```

---

## Button

> A polymorphic, accessible button that renders as `<button>`, `<a>`, or `<span>`. Visual theme applied via className. Supports icons, loading states, tooltips, and grouping.

### Import

```tsx
import { Button, ButtonGroup } from "@chumlab/ui/button";
```

### Basic Usage

```tsx
<Button
  className="bg-indigo-600 text-white hover:bg-indigo-500 shadow-md"
  onClick={() => console.log("clicked")}
>
  Save Changes
</Button>
```

### Button Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `as` | `"button" \| "a" \| "span"` | `"button"` | Rendered element type. |
| `asChild` | `boolean` | `false` | Merge props onto child element via Slot. |
| `className` | `string` | — | Visual styling classes (colors, shadows, borders). |
| `size` | `"sm" \| "md" \| "lg"` | — | Emits data-size for CSS targeting. No built-in sizing. |
| `disabled` | `boolean` | `false` | Disables interaction. |
| `loading` | `boolean` | `false` | Shows loader, disables button. |
| `loadingText` | `ReactNode` | — | Text shown during loading. |
| `loader` | `ReactNode` | — | Custom loader element. |
| `loaderPosition` | `"left" \| "right"` | `"right"` | Loader placement. |
| `loaderSize` | `number` | `16` | Loader size in px. |
| `fullWidth` | `boolean` | `false` | Stretch to container width. |
| `startIcon` | `ReactNode` | — | Icon before label. |
| `endIcon` | `ReactNode` | — | Icon after label. |
| `iconAnimation` | `IconAnimation` | `"none"` | Animation type for icons. |
| `animateOnHover` | `boolean` | `true` | true = hover-only, false = continuous. |
| `animateIcon` | `"leading" \| "trailing" \| "both"` | `"trailing"` | Which icon(s) to animate. |
| `reduceMotion` | `boolean \| "auto"` | `"auto"` | Respects OS preference. |
| `tooltip` | `ReactNode` | — | Hover tooltip content. |
| `tooltipProps` | `ButtonTooltipProps` | — | Tooltip config. |
| `classes` | `ButtonClasses` | — | Per-slot overrides: root, content, startIcon, endIcon, loader. |
| `unstyled` | `boolean` | `false` | Strip all default classes. |

### ButtonGroup Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `orientation` | `"horizontal" \| "vertical"` | `"horizontal"` | Layout direction. |
| `className` | `string` | — | Container styling. |

### Prop Constraints

- `as="a"` requires `href`. TypeScript will error without it.
- `asChild` is mutually exclusive with `as`.
- `loading` makes the button disabled automatically.
- Button defaults are structural only (padding, focus ring). Colors come from `className`.

### Class Slots

```
root (button/a/span — structural styles + your className)
└── content (inline-flex wrapper)
    ├── loader (optional)
    ├── startIcon
    ├── children
    └── endIcon
```

### Data Attributes

- `data-loading="true"` — when loading
- `data-disabled="true"` — when disabled or loading
- `data-full-width="true"` — when fullWidth
- `data-size="sm|md|lg"` — size prop value

### Advanced Pattern: Async loading

```tsx
const [saving, setSaving] = useState(false);
<Button
  className="bg-indigo-600 text-white"
  loading={saving}
  loadingText="Saving..."
  onClick={async () => { setSaving(true); await saveData(); setSaving(false); }}
>
  Save
</Button>
```

---

## CascadingDropdown

> A multi-level dropdown menu with nested submenus. Supports single and multi-select per level, async data loading, and search with debounce.

### Import

```tsx
import { CascadingDropdown } from "@chumlab/ui/cascading-dropdown";
import type { CascadingOption, CascadingValue } from "@chumlab/ui/cascading-dropdown";
```

### Basic Usage

```tsx
const [value, setValue] = useState<CascadingValue>({});
<CascadingDropdown
  options={[
    { value: "electronics", label: "Electronics", children: [
      { value: "phones", label: "Phones" },
      { value: "laptops", label: "Laptops" },
    ]},
    { value: "clothing", label: "Clothing", children: [
      { value: "shirts", label: "Shirts" },
    ]},
  ]}
  value={value}
  onValueChange={setValue}
  placeholder="Select category..."
/>
```

### CascadingDropdown Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `options` | `CascadingOption[]` | **required** | Top-level menu items with optional children. |
| `value` | `CascadingValue` | — | Controlled value: { parentValue: "child" \| string[] }. |
| `defaultValue` | `CascadingValue` | — | Uncontrolled initial value. |
| `onValueChange` | `(value, path) => void` | — | Selection change callback. |
| `onLoadChildren` | `(parent) => Promise<CascadingOption[]>` | — | Async loader for children on hover. |
| `onLoadError` | `(error) => void` | — | Async child loading error callback. |
| `open` | `boolean` | — | Controlled open state. |
| `defaultOpen` | `boolean` | — | Uncontrolled initial open state. |
| `onOpenChange` | `(open) => void` | — | Open/close callback. |
| `placeholder` | `ReactNode` | — | Trigger placeholder text. |
| `disabled` | `boolean` | `false` | Disables the dropdown. |
| `error` | `boolean` | `false` | Error state. |
| `errorMessage` | `ReactNode` | — | Error message below trigger. |
| `label` | `ReactNode` | — | Label above trigger. |
| `description` | `ReactNode` | — | Description text. |
| `success` | `boolean` | `false` | Success state. |
| `successMessage` | `ReactNode` | — | Success message. |
| `required` | `boolean` | `false` | Marks as required. |
| `clearable` | `boolean` | `false` | Shows clear button. |
| `showChevron` | `boolean` | `true` | Shows chevron on trigger. |
| `submenuPosition` | `"right" \| "left"` | `"right"` | Submenu direction. |
| `dropdownPosition` | `"top" \| "bottom"` | `"bottom"` | Dropdown direction. |
| `closeOnSelect` | `boolean` | `true` | Close on selection. |
| `lockScroll` | `boolean` | `false` | Prevent page scroll when open. |
| `fullWidth` | `boolean` | `false` | Trigger takes full width. |
| `classes` | `CascadingDropdownClasses` | — | Per-slot class overrides. |
| `unstyled` | `boolean` | `false` | Remove all default styles. |
| `showMenuSearch` | `boolean` | `false` | Search input in menu. |
| `showSubmenuSearch` | `boolean` | `false` | Search input in submenus. |
| `onMenuSearch` | `(query) => Promise<CascadingOption[]>` | — | Async menu search. |
| `onSubmenuSearch` | `(query, parent) => Promise<CascadingOption[]>` | — | Async submenu search. |
| `searchDebounceMs` | `number` | `300` | Debounce delay for async search. |
| `menuSearchPlaceholder` | `string` | — | Menu search placeholder. |
| `submenuSearchPlaceholder` | `string` | — | Submenu search placeholder. |
| `shimmerCount` | `number` | `5` | Shimmer items during loading. |
| `keepMounted` | `boolean` | `false` | Keep portal in DOM when closed. |
| `portalContainer` | `HTMLElement \| null` | `document.body` | Portal render target. |
| `dropdownZIndex` | `number` | — | z-index for dropdown portal. |
| `dropdownGap` | `number` | — | Gap between trigger and dropdown. |

### CascadingOption (data shape)

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `value` | `string` | **required** | Unique value identifier. |
| `label` | `string` | **required** | Display text. |
| `content` | `ReactNode` | — | Custom render for menu item. |
| `selectedContent` | `ReactNode` | — | Custom render when selected. |
| `disabled` | `boolean` | `false` | Disable this option. |
| `children` | `CascadingOption[]` | — | Nested submenu options. |
| `selectionMode` | `"single" \| "multi"` | `"single"` | Selection mode for children. |
| `hasChildren` | `boolean` | — | Hint for async: triggers onLoadChildren. |

### Prop Constraints

- `selectionMode` set on each parent. "single" = radio, "multi" = checkbox.
- `onLoadChildren` only called when `hasChildren` is true and `children` is undefined.
- `onMenuSearch` disables client-side filtering when provided.
- Value shape: `{ parentValue: "childValue" }` for single, `{ parentValue: ["a","b"] }` for multi.

### Class Slots

```
root > wrapper
├── label, description
├── trigger [chevron, clearIcon]
├── errorMessage / successMessage
└── portal > menu
    ├── searchInput [searchIcon + searchInputElement]
    ├── menuItem [submenuChevron]
    │   └── submenuContainer > submenu
    │       ├── submenuSearchInput
    │       └── submenuItem [checkbox, checkIcon]
    ├── noResults
    └── shimmer > shimmerItem
```

### Data Attributes

- `data-disabled`, `data-error`, `data-open` on trigger
- `role="menu"`, `role="menuitem"`, `role="menuitemcheckbox"`, `role="menuitemradio"`
- `aria-expanded`, `aria-checked` on items

### Advanced Pattern: Async data loading

```tsx
<CascadingDropdown
  options={[
    { value: "electronics", label: "Electronics", hasChildren: true },
  ]}
  onLoadChildren={async (parent) => {
    const res = await fetch(`/api/categories/${parent.value}/children`);
    return res.json();
  }}
  value={value}
  onValueChange={setValue}
/>
```

---

## Checkbox

> A customizable checkbox with label, description, indeterminate state, custom icons, sizes, shapes, and native input.

### Import

```tsx
import { Checkbox } from "@chumlab/ui/checkbox";
```

### Basic Usage

```tsx
const [checked, setChecked] = useState(false);
<Checkbox label="I agree to the terms" checked={checked} onValueChange={setChecked} />
```

### Checkbox Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `label` | `ReactNode` | — | Text beside the checkbox. |
| `description` | `ReactNode` | — | Helper text below the label. |
| `checked` | `boolean` | — | Controlled checked state. |
| `defaultChecked` | `boolean` | — | Initial state (uncontrolled). |
| `indeterminate` | `boolean` | `false` | Shows minus icon instead of check. |
| `onValueChange` | `(checked: boolean) => void` | — | Fires on state change. |
| `disabled` | `boolean` | `false` | Disables the checkbox. |
| `required` | `boolean` | `false` | Marks as required. |
| `error` | `boolean` | `false` | Error state. |
| `errorMessage` | `ReactNode` | — | Error text. |
| `success` | `boolean` | `false` | Success state. |
| `successMessage` | `ReactNode` | — | Success text. |
| `loading` | `boolean` | `false` | Loading state. |
| `size` | `CheckboxSize` | `"md"` | xs/sm/md/lg/xl or pixel number. |
| `shape` | `CheckboxShape` | `"rounded"` | square/rounded/circle. |
| `checkedIcon` | `ReactNode` | Built-in check | Custom icon when checked. |
| `uncheckedIcon` | `ReactNode` | — | Custom icon when unchecked. |
| `indeterminateIcon` | `ReactNode` | Built-in minus | Custom icon when indeterminate. |
| `classes` | `CheckboxClasses` | — | Per-slot class overrides. |
| `unstyled` | `boolean` | `false` | Remove all default styles. |
| `reduceMotion` | `boolean \| "auto"` | `"auto"` | Motion preference. |
| `name` | `string` | — | Form field name. |
| `id` | `string` | auto | Checkbox input ID. |

### Prop Constraints

- `checked` + `onValueChange` for controlled mode. Must provide both.
- `indeterminate` overrides checked visual. Underlying input is still checked/unchecked.
- `classes.checked` / `classes.unchecked` REPLACE per slot. Not additive.

### Class Slots

```
root > label
├── checkbox [hidden input + icon]
└── labelContainer > label + description
error / success (below)
```

### Data Attributes

- `data-checked`, `data-indeterminate`, `data-disabled`, `data-error`, `data-size`, `data-shape`

### Advanced Pattern: Indeterminate "Select All"

```tsx
const [items, setItems] = useState([false, false, false]);
const allChecked = items.every(Boolean);
const someChecked = items.some(Boolean);
<Checkbox
  label="Select All"
  checked={allChecked}
  indeterminate={someChecked && !allChecked}
  onValueChange={(checked) => setItems(items.map(() => checked))}
/>
```

---

## CountryFlag

> Renders a country flag image from a two-letter ISO country code. Supports sizes, tooltips, grouping, shimmer loading, and error fallback.

### Import

```tsx
import { CountryFlag, CountryFlagGroup, CountryFlagShimmer } from "@chumlab/ui/country-flag";
```

### Basic Usage

```tsx
<CountryFlag code="us" size="lg" />
<CountryFlag code="gb" size="lg" />
<CountryFlag code="jp" size="lg" />
```

### CountryFlag Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `code` | `string` | **required** | Two-letter ISO country code. |
| `size` | `CountryFlagSize \| number` | `"md"` | Flag size. |
| `aspectRatio` | `number` | `0.75` | Height/width ratio. |
| `alt` | `string` | code | Accessible alt text. |
| `fallback` | `ReactNode` | — | Shown on image error. |
| `loading` | `boolean` | `false` | Show shimmer instead of flag. |
| `tooltip` | `ReactNode \| CountryFlagTooltipConfig` | — | Tooltip on hover. |
| `basePath` | `string` | `"/flags"` | Directory for SVG files. |
| `classes` | `CountryFlagClasses` | — | Per-slot class overrides. |
| `unstyled` | `boolean` | `false` | Remove all default styles. |
| `reduceMotion` | `boolean \| "auto"` | `"auto"` | Motion preference. |

### CountryFlagGroup Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `max` | `number` | — | Max visible flags before "+N" count. |
| `size` | `CountryFlagSize \| number` | — | Size for all children. |
| `showCountTooltip` | `boolean` | — | Tooltip on surplus count. |
| `renderSurplus` | `(count) => ReactNode` | — | Custom surplus renderer. |
| `classes` | `CountryFlagGroupClasses` | — | Per-slot class overrides. |

### Class Slots

```
root (span, role="img")
├── image (img)
└── fallback (when error)
```

### Data Attributes

- `data-country` — ISO country code
- `data-size` — preset size name
- `data-loaded` — when image loaded
- `data-error` — when image failed

### Advanced Pattern: Flag group with overflow

```tsx
<CountryFlagGroup max={3} size="md">
  <CountryFlag code="us" />
  <CountryFlag code="gb" />
  <CountryFlag code="de" />
  <CountryFlag code="fr" />
  <CountryFlag code="jp" />
</CountryFlagGroup>
```

---

## DatePicker

> A full-featured date picker supporting single, range, and multiple date selection with calendar popover, presets, markers, locale support, and keyboard navigation.

### Import

```tsx
import { DatePicker } from "@chumlab/ui/date-picker";
```

### Basic Usage

```tsx
const [date, setDate] = useState<Date | null>(null);
<DatePicker value={date} onValueChange={(d) => setDate(d)} label="Select a date" placeholder="Pick a date..." />
```

### DatePicker Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `mode` | `"single" \| "range" \| "multiple"` | `"single"` | Selection mode. |
| `value` | `Date \| DateRange \| Date[] \| null` | — | Controlled value (type matches mode). |
| `onValueChange` | `(value, dateValue) => void` | — | Selection change callback. |
| `onClear` | `() => void` | — | Clear callback. |
| `onMonthChange` | `(month: Date) => void` | — | Displayed month change callback. |
| `open` | `boolean` | — | Controlled open state. |
| `defaultOpen` | `boolean` | `false` | Open on mount (uncontrolled). |
| `onOpenChange` | `(open: boolean) => void` | — | Open state callback. |
| `minDate` | `Date` | — | Earliest selectable date. |
| `maxDate` | `Date` | — | Latest selectable date. |
| `disabledDates` | `DisabledDateOptions` | — | Fine-grained date disabling. |
| `weekStartsOn` | `0-6` | `0` | Day the week starts on (0 = Sunday). |
| `locale` | `Locale` | — | date-fns locale for month/day names. |
| `numberOfMonths` | `number` | `1` | Months displayed side by side. |
| `dateFormat` | `string` | `"MMM d, yyyy"` | Date format string (date-fns). |
| `showTodayIndicator` | `boolean` | `true` | Ring on today's date. |
| `showTodayButton` | `boolean` | `false` | "Today" button in footer. |
| `showWeekNumbers` | `boolean` | `false` | ISO week numbers column. |
| `showOutsideDays` | `boolean` | `true` | Days from adjacent months. |
| `outsideDaysSelectable` | `boolean` | `true` | Allow selecting outside days. |
| `fixedWeeks` | `boolean` | `false` | Always show 6 weeks per month. |
| `showPresets` | `boolean` | `false` | Show preset buttons panel. |
| `presets` | `DatePreset[]` | — | Custom presets. |
| `markers` | `DateMarker[]` | — | Dates with indicator dots/tooltips. |
| `showMarkerIndicator` | `boolean` | `true` | Colored dots on marked days. |
| `showMarkerTooltip` | `boolean` | `true` | Tooltip on marked days. |
| `placeholder` | `string` | — | Placeholder text. |
| `disabled` | `boolean` | `false` | Disable the picker. |
| `error` | `boolean` | `false` | Error state. |
| `errorMessage` | `ReactNode` | — | Error message. |
| `label` | `ReactNode` | — | Label above trigger. |
| `description` | `ReactNode` | — | Description below label. |
| `success` | `boolean` | `false` | Success state. |
| `successMessage` | `ReactNode` | — | Success message. |
| `loading` | `boolean` | `false` | Loading state. |
| `clearable` | `boolean` | — | Alias for showClearButton. |
| `showClearButton` | `boolean` | `true` | Show clear button. |
| `showCalendarIcon` | `boolean` | `true` | Show calendar icon. |
| `required` | `boolean` | `false` | Required field. |
| `fullWidth` | `boolean` | `false` | Full width trigger. |
| `classes` | `DatePickerClasses` | — | Per-slot class overrides. |
| `unstyled` | `boolean` | `false` | Strip all defaults. |
| `portalContainer` | `HTMLElement \| null` | — | Portal target. |
| `lockScroll` | `boolean` | `false` | Lock body scroll. |
| `dropdownZIndex` | `number` | `50` | Calendar z-index. |
| `dropdownPosition` | `"top" \| "bottom"` | `"bottom"` | Calendar placement. |
| `forceDropdownPosition` | `boolean` | `false` | Prevent auto-flipping. |
| `dropdownGap` | `number` | `4` | Gap between trigger and calendar. |
| `keepMounted` | `boolean` | `false` | Keep calendar mounted when closed. |
| `reduceMotion` | `boolean \| "auto"` | `"auto"` | Respects OS setting. |
| `name` | `string` | — | Form field name. |

### DisabledDateOptions

| Field | Type | Description |
|-------|------|-------------|
| `before` | `Date` | Disable all dates before this. |
| `after` | `Date` | Disable all dates after this. |
| `disablePast` | `boolean` | Disable all past dates. |
| `disableFuture` | `boolean` | Disable all future dates. |
| `dates` | `Date[]` | Specific dates to disable. |
| `daysOfWeek` | `number[]` | Days of week to disable (0-6). |
| `custom` | `(date: Date) => boolean` | Custom function, true = disabled. |

### DateMarker

| Field | Type | Description |
|-------|------|-------------|
| `date` | `Date` | **Required.** The date to mark. |
| `label` | `string` | **Required.** Tooltip title. |
| `description` | `string` | Optional tooltip description. |
| `type` | `string` | Optional type string (e.g. "meeting"). |
| `color` | `string` | Optional CSS color for indicator dot. |

### DatePreset

| Field | Type | Description |
|-------|------|-------------|
| `label` | `string` | **Required.** Button text. |
| `getValue` | `() => Date \| DateRange \| Date[]` | **Required.** Returns value to set. |

### Prop Constraints

- `mode` determines `value` and `onValueChange` types. "single" = Date | null, "range" = DateRange | null, "multiple" = Date[] | null.
- `showPresets` only shows presets if true. Presets must match mode.
- `markers` only visible when `showMarkerIndicator` is true.
- Never combine `open` + `defaultOpen`.

### Class Slots

Key slots: `trigger`, `input`, `calendarIcon`, `clearButton`, `calendar`, `header`, `day`, `daySelected`, `dayToday`, `dayDisabled`, `dayOutside`, `dayRangeStart`, `dayRangeEnd`, `dayRangeMiddle`, `dayFocused`, `dayMarked`, `markerIndicator`, `markerTooltip`, `presets`, `presetButton`, `presetActive`, `footer`, `todayButton`, `monthGrid`, `grid`, `label`, `description`, `error`, `success`.

### Data Attributes

- `data-selected`, `data-today`, `data-disabled`, `data-outside`, `data-range-start`, `data-range-end`, `data-in-range`, `data-focused`, `data-marked`, `data-marker-type` — on day cells
- `data-disabled`, `data-error`, `data-open`, `data-loading`, `data-success` — on root

### Advanced Pattern: Range selection with presets

```tsx
const [range, setRange] = useState<DateRange | null>(null);
<DatePicker mode="range" value={range} onValueChange={(r) => setRange(r)} showPresets showClearButton />
```

---

## Drawer

> A sliding panel overlay with focus trapping, scroll lock, swipe-to-close, snap points, and stacked drawer support.

### Import

```tsx
import { Drawer, DrawerHeader, DrawerBody, DrawerFooter, DrawerCloseButton } from "@chumlab/ui/drawer";
```

### Basic Usage

```tsx
const [open, setOpen] = useState(false);
<>
  <button onClick={() => setOpen(true)}>Open Drawer</button>
  <Drawer open={open} onOpenChange={setOpen}>
    <DrawerHeader><h2>Settings</h2><DrawerCloseButton /></DrawerHeader>
    <DrawerBody><p>Drawer content goes here.</p></DrawerBody>
    <DrawerFooter><button onClick={() => setOpen(false)}>Close</button></DrawerFooter>
  </Drawer>
</>
```

### Drawer Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `open` | `boolean` | — | Controlled open state. |
| `onOpenChange` | `(open: boolean) => void` | — | Open/close callback. |
| `direction` | `"left" \| "right" \| "top" \| "bottom"` | `"right"` | Slide direction. |
| `snapPoints` | `number[]` | — | Percentage snap points (0-1). Requires swipeable. |
| `swipeable` | `boolean` | `false` | Enable swipe-to-close. |
| `trapFocus` | `boolean` | `true` | Trap keyboard focus inside. |
| `keepMounted` | `boolean` | `false` | Keep DOM alive when closed. |
| `modal` | `boolean` | `true` | Modal behavior with overlay. |
| `lockScroll` | `boolean` | `true` | Lock body scroll. |
| `closeOnEscape` | `boolean` | `true` | Close on Escape key. |
| `closeOnOverlayClick` | `boolean` | `true` | Close on overlay click. |
| `classes` | `DrawerClasses` | — | Per-slot class overrides. |
| `unstyled` | `boolean` | `false` | Strip all defaults. |

### Prop Constraints

- `snapPoints` requires `swipeable={true}`.
- `trapFocus` defaults true when `modal={true}`.
- `keepMounted` uses `inert` to hide when closed.

### Data Attributes

- `role="dialog"` with `aria-modal` when modal

### Advanced Pattern: Swipeable bottom sheet

```tsx
<Drawer open={open} onOpenChange={setOpen} direction="bottom" swipeable snapPoints={[0.5, 1]}>
  <DrawerHeader><h2>Details</h2><DrawerCloseButton /></DrawerHeader>
  <DrawerBody>Content here</DrawerBody>
</Drawer>
```

---

## Dropdown

> A single-select dropdown (combobox) with portal rendering, keyboard navigation, async loading, and full style customization.

### Import

```tsx
import { Dropdown } from "@chumlab/ui/dropdown";
import type { DropdownOption } from "@chumlab/ui/dropdown";
```

### Basic Usage

```tsx
const [value, setValue] = useState<string | null>(null);
<Dropdown
  options={[
    { value: "apple", label: "Apple" },
    { value: "banana", label: "Banana" },
  ]}
  value={value}
  onValueChange={(v) => setValue(v)}
  placeholder="Select a fruit..."
/>
```

### Dropdown Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `options` | `DropdownOption[]` | `[]` | Selectable options. |
| `value` | `string \| null` | — | Controlled selected value. |
| `defaultValue` | `string` | — | Uncontrolled initial value. |
| `onValueChange` | `(value, option) => void` | — | Selection change callback. |
| `open` | `boolean` | — | Controlled open state. |
| `defaultOpen` | `boolean` | — | Uncontrolled initial open state. |
| `onOpenChange` | `(open: boolean) => void` | — | Open state callback. |
| `placeholder` | `ReactNode` | `"Select an option"` | Trigger text when nothing selected. |
| `disabled` | `boolean` | `false` | Disable the dropdown. |
| `error` | `boolean` | `false` | Error state. |
| `errorMessage` | `ReactNode` | — | Error message with role="alert". |
| `label` | `ReactNode` | — | Label above trigger. |
| `description` | `ReactNode` | — | Helper text. |
| `success` | `boolean` | `false` | Success state. |
| `successMessage` | `ReactNode` | — | Success message. |
| `required` | `boolean` | `false` | Required field. |
| `clearable` | `boolean` | `false` | Clear button + deselect. |
| `showChevron` | `boolean` | `true` | Show/hide chevron. |
| `showSelectedIcon` | `boolean` | `true` | Check icon on selected. |
| `fullWidth` | `boolean` | `false` | Full container width. |
| `loading` | `boolean` | `false` | Loading state (shimmer). |
| `onLoadOptions` | `() => Promise<DropdownOption[]>` | — | Async option loader. |
| `loadOnOpen` | `boolean` | `false` | Trigger onLoadOptions on open. |
| `shimmerCount` | `number` | `5` | Shimmer rows. |
| `unstyled` | `boolean` | `false` | Remove defaults. |
| `classes` | `DropdownClasses` | — | Per-slot overrides. |
| `keepMounted` | `boolean` | `false` | Keep popup in DOM. |
| `portalContainer` | `HTMLElement \| null` | `document.body` | Portal target. |
| `lockScroll` | `boolean` | `false` | Lock body scroll. |
| `dropdownPosition` | `"top" \| "bottom"` | `"bottom"` | Preferred direction (auto-flips). |
| `forceDropdownPosition` | `boolean` | `false` | Lock position. |
| `dropdownZIndex` | `number` | `50` | Portal z-index. |
| `dropdownGap` | `number` | `4` | Gap between trigger and popup. |
| `typeaheadTimeout` | `number` | `500` | Typeahead buffer reset (ms). |
| `renderTrigger` | `(props) => ReactNode` | — | Custom trigger renderer. |
| `name` | `string` | — | Form field name (hidden input). |

### DropdownOption (data shape)

| Prop | Type | Description |
|------|------|-------------|
| `value` | `string` | **Required.** Unique identifier. |
| `label` | `string` | **Required.** Display text. |
| `content` | `ReactNode` | Custom render in option list. |
| `selectedContent` | `ReactNode` | Custom render in trigger when selected. |
| `disabled` | `boolean` | Disable this option. |

### Prop Constraints

- Never combine `value` + `defaultValue`.
- `value` requires `onValueChange` — controlled mode.
- `onLoadOptions` requires `loadOnOpen={true}` to trigger.
- `renderTrigger` — must spread all provided props and attach `ref`.

### Class Slots

```
root > label + description + wrapper
├── trigger > triggerText + chevron
├── clearIcon
└── content (portal) > optionList > option [checkIcon]
    noResults | shimmer > shimmerItem
error | success
```

### Data Attributes

- `data-disabled`, `data-error`, `data-open`, `data-success` on root
- `data-selected`, `data-focused`, `data-disabled`, `data-value` on options
- `data-state="open|closed"`, `data-position="top|bottom"` on content

### Advanced Pattern: Async loading on open

```tsx
<Dropdown
  loadOnOpen
  onLoadOptions={async () => { const res = await fetch("/api/options"); return res.json(); }}
  placeholder="Click to load..."
/>
```

---

## Input

> A production-grade text input with labels, icons, prefix/suffix, clearable, character count, and validation states.

### Import

```tsx
import { Input } from "@chumlab/ui/input";
```

### Basic Usage

```tsx
<Input label="Full Name" placeholder="Enter your name..." />
<Input label="Email" placeholder="you@example.com" description="We'll never share your email" />
```

### Input Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `label` | `ReactNode` | — | Label above the input. |
| `description` | `ReactNode` | — | Helper text below label. |
| `placeholder` | `string` | — | Placeholder text. |
| `error` | `boolean` | `false` | Error state. |
| `errorMessage` | `ReactNode` | — | Error message. |
| `success` | `boolean` | `false` | Success state. |
| `successMessage` | `ReactNode` | — | Success message. |
| `size` | `InputSize` | — | Visual size (data attribute only). |
| `prefix` | `ReactNode` | — | Static content before input (e.g. "$"). |
| `suffix` | `ReactNode` | — | Static content after input (e.g. ".com"). |
| `startIcon` | `ReactNode` | — | Icon before input. |
| `endIcon` | `ReactNode` | — | Icon after input. |
| `onStartIconClick` | `() => void` | — | Makes startIcon a button. |
| `onEndIconClick` | `() => void` | — | Makes endIcon a button. |
| `startIconLabel` | `string` | — | Aria label for clickable startIcon. |
| `endIconLabel` | `string` | — | Aria label for clickable endIcon. |
| `onValueChange` | `(value: string) => void` | — | Simple value callback. |
| `clearable` | `boolean` | `false` | Show clear button. |
| `onClear` | `() => void` | — | Clear button callback. |
| `showCount` | `boolean` | `false` | Show character count. |
| `loading` | `boolean` | `false` | Loading state. |
| `fullWidth` | `boolean` | `false` | Full width container. |
| `classes` | `InputClasses` | — | Per-slot class overrides. |
| `unstyled` | `boolean` | `false` | Remove all default styles. |
| `required` | `boolean` | — | Required field. |
| `disabled` | `boolean` | — | Disabled state. |
| `readOnly` | `boolean` | — | Read-only state. |
| `maxLength` | `number` | — | Max characters. |

### Prop Constraints

- `showCount` requires `maxLength` to be set.
- `onStartIconClick` requires `startIconLabel` for accessibility.

### Class Slots

```
root > label + description + wrapper(prefix + startIcon + input + endIcon + suffix + clear + loader) + error/success + count
```

### Data Attributes

- `data-size`, `data-disabled`, `data-error`, `data-success`, `data-readonly` on root

### Advanced Pattern: Password with toggle

```tsx
const [show, setShow] = useState(false);
<Input
  type={show ? "text" : "password"}
  label="Password"
  endIcon={show ? <EyeOff /> : <Eye />}
  onEndIconClick={() => setShow(!show)}
  endIconLabel={show ? "Hide password" : "Show password"}
/>
```

---

## InternationalPhoneInput

> Phone number input with country code selection, automatic formatting, paste detection, copy formats, and full style customization.

### Import

```tsx
import { InternationalPhoneInput } from "@chumlab/ui/international-phone-input";
import type { PhoneNumberData } from "@chumlab/ui/international-phone-input";
```

### Basic Usage

```tsx
const [phone, setPhone] = useState<PhoneNumberData | undefined>();
<InternationalPhoneInput label="Phone Number" value={phone} onValueChange={setPhone} placeholder="Enter phone number" />
```

### InternationalPhoneInput Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `value` | `PhoneNumberValue` | — | Controlled phone value { countryCode, phoneNumber }. |
| `defaultValue` | `PhoneNumberValue` | — | Initial value (uncontrolled). |
| `onValueChange` | `(data: PhoneNumberData) => void` | — | Change callback with full validation data. |
| `onCountryChange` | `(country: CountryOption) => void` | — | Country change callback. |
| `defaultCountry` | `string` | `"us"` | Default country code. |
| `countries` | `CountryOption[]` | `DEFAULT_COUNTRIES` | Available countries. |
| `preferredCountries` | `string[]` | `["us","gb","ca","au"]` | Countries shown at top. |
| `label` | `ReactNode` | — | Label. |
| `description` | `ReactNode` | — | Helper text. |
| `required` | `boolean` | `false` | Required field. |
| `disabled` | `boolean` | `false` | Disabled. |
| `readOnly` | `boolean` | `false` | Read-only. |
| `error` | `boolean` | `false` | Error state. |
| `errorMessage` | `ReactNode` | — | Error message. |
| `success` | `boolean` | `false` | Success state. |
| `successMessage` | `ReactNode` | — | Success message. |
| `placeholder` | `string` | `"Enter phone number"` | Placeholder. |
| `fullWidth` | `boolean` | `false` | Full width. |
| `loading` | `boolean` | `false` | Loading state. |
| `clearable` | `boolean` | `false` | Show clear button. |
| `validateOnBlur` | `boolean` | `true` | Validate on blur. |
| `validationMessage` | `ReactNode` | — | Custom validation message. |
| `enablePasteDetection` | `boolean` | `false` | Auto-detect country from paste. |
| `copyFormat` | `"e164" \| "international" \| "national"` | `"e164"` | Copy format. |
| `onPasteDetected` | `(data: PasteDetectedData) => void` | — | Paste detection callback. |
| `formatPatterns` | `Record<string, PhoneFormatPattern>` | — | Custom formatting patterns. |
| `lengthRules` | `Record<string, PhoneLengthRule>` | — | Custom length validation rules. |
| `flagSize` | `number` | `22` | Pixel width of the flag in trigger and dropdown options. Height auto-scales at the 22:17 aspect ratio. |
| `renderCountryOption` | `(props) => ReactNode` | — | Custom country option renderer. |
| `renderSelectedCountry` | `(country) => ReactNode` | — | Custom selected country renderer. |
| `dropdownPosition` | `"top" \| "bottom"` | `"bottom"` | Country dropdown placement. |
| `forceDropdownPosition` | `boolean` | `false` | Lock dropdown position. |
| `dropdownZIndex` | `number` | `50` | Dropdown z-index. |
| `lockScroll` | `boolean` | `false` | Lock body scroll. |
| `classes` | `InternationalPhoneInputClasses` | — | Slot-based class overrides. |
| `unstyled` | `boolean` | `false` | Strip all defaults. |
| `name` | `string` | — | Renders hidden inputs: {name} + {name}_country. |

### Prop Constraints

- Never combine `value` + `defaultValue`.
- `enablePasteDetection` must be true for `onPasteDetected` to fire.
- `copyFormat` only affects Ctrl+C behavior, not display.
- `formatPatterns` extends (not replaces) built-in patterns.
- `name` renders two hidden inputs: `{name}` (full number) and `{name}_country`.

### Class Slots

```
root > label + description + wrapper(group)
├── countrySelect (SearchableDropdown)
│   ├── countrySelectTrigger
│   ├── countrySelectDropdown (portal)
│   │   ├── countrySelectSearchInput
│   │   └── countrySelectOptionList > countrySelectOption
│   └── countrySelectChevron
├── input
└── clearButton
error | success
```

### Data Attributes

- `data-disabled`, `data-error`, `data-success`, `data-readonly`, `data-full-width`, `data-loading` on root

### Advanced Pattern: Paste detection with country auto-switch

```tsx
<InternationalPhoneInput
  enablePasteDetection
  onPasteDetected={(data) => console.log(data.detectedCountry?.name)}
  copyFormat="e164"
/>
```

---

## Loader

> Animated loading indicators. Four variants: CircularLoader, LinearLoader, DotLoader, PulseLoader.

### Import

```tsx
import { CircularLoader, LinearLoader, DotLoader, PulseLoader } from "@chumlab/ui/loader";
```

### Basic Usage

```tsx
<CircularLoader />
<LinearLoader />
<DotLoader />
<PulseLoader />
```

### Shared Props (all variants)

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `size` | `number` | varies | Size in pixels. |
| `color` | `string` | `"currentColor"` | Stroke/fill color. |
| `speed` | `number` | `1` | Animation speed multiplier. |
| `className` | `string` | — | Additional CSS classes. |
| `classes` | `*LoaderClasses` | — | Per-slot class overrides. |
| `unstyled` | `boolean` | `false` | Remove default styles. |

### Class Slots

Circular: `root`, `svg`, `track`, `indicator`. Linear: `root`, `bar`. Dot: `root`, `dot`. Pulse: `root`, `ring`.

### Data Attributes

- `role="status"`, `aria-live="polite"`, `aria-label="Loading"` on all variants

### Advanced Pattern: Inline button loading

```tsx
<Button loading loader={<CircularLoader size={14} color="white" />}>Saving...</Button>
```

---

## Modal

> Accessible dialog overlay with focus trapping, scroll lock, nested modals, animations, and full keyboard navigation.

### Import

```tsx
import { Modal, useModal } from "@chumlab/ui/modal";
```

### Basic Usage

```tsx
const [open, setOpen] = useState(false);
<>
  <button onClick={() => setOpen(true)}>Open Modal</button>
  <Modal open={open} onOpenChange={setOpen} title="Welcome Back">
    <p>Your session has been restored.</p>
    <div style={{ display: "flex", justifyContent: "flex-end", gap: 8, marginTop: 16 }}>
      <button onClick={() => setOpen(false)}>Cancel</button>
      <button onClick={() => setOpen(false)}>Continue</button>
    </div>
  </Modal>
</>
```

### Modal Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `open` | `boolean` | — | Controlled open state. |
| `onOpenChange` | `(open: boolean) => void` | — | State change callback. |
| `defaultOpen` | `boolean` | `false` | Initial open state. |
| `children` | `ReactNode` | **required** | Modal body content. |
| `title` | `ReactNode` | — | Title in header. |
| `description` | `ReactNode` | — | Description below title. |
| `icon` | `ReactNode` | — | Icon in header (requires showIcon). |
| `showIcon` | `boolean` | `false` | Show header icon. |
| `showCloseButton` | `boolean` | `true` | Show close button. |
| `closeIcon` | `ReactNode` | — | Custom close icon. |
| `showHeader` | `boolean` | `true` | Render header section. |
| `showOverlay` | `boolean` | `true` | Render backdrop overlay. |
| `preventOutsideClick` | `boolean` | `false` | Prevent closing on overlay click. |
| `closeOnEscape` | `boolean` | `true` | Close on Escape key. |
| `lockScroll` | `boolean` | `true` | Lock body scroll. |
| `trapFocus` | `boolean` | `true` | Trap keyboard focus inside. |
| `restoreFocus` | `boolean` | `true` | Restore focus on close. |
| `initialFocus` | `RefObject<HTMLElement>` | — | Element to focus on open. |
| `keepMounted` | `boolean` | `false` | Keep DOM when closed. |
| `maxWidth` | `string \| number` | — | Max width. |
| `maxHeight` | `string \| number` | — | Max height. |
| `minWidth` | `string \| number` | — | Min width. |
| `minHeight` | `string \| number` | — | Min height. |
| `fullScreen` | `boolean` | `false` | Fill viewport. |
| `centered` | `boolean` | `true` | Center vertically. |
| `overlayColor` | `string` | `"black"` | Overlay color. |
| `overlayOpacity` | `number` | `0.5` | Overlay opacity. |
| `animationDuration` | `number` | `200` | Animation duration (ms). |
| `disableAnimation` | `boolean` | `false` | Disable animations. |
| `reduceMotion` | `boolean \| "auto"` | `"auto"` | Motion preference. |
| `nestingLevel` | `number` | auto | Nesting depth. |
| `maxNestingLevel` | `number` | `5` | Maximum nesting depth. |
| `zIndex` | `number` | `9999+` | Custom z-index. |
| `classes` | `ModalClasses` | — | Per-slot class overrides. |
| `unstyled` | `boolean` | `false` | Strip all defaults. |
| `className` | `string` | — | Additional class on content. |
| `contentStyle` | `CSSProperties` | — | Inline styles on content. |

### Prop Constraints

- `open` + `onOpenChange` both needed for controlled mode.
- `preventOutsideClick` + `closeOnEscape={false}` for mandatory action modals.
- `showHeader={false}` hides title, description, and close button.
- `fullScreen` overrides maxWidth/maxHeight.
- `nestingLevel` auto-managed via context; set manually only for external nesting.

### Class Slots

```
root (fixed inset-0)
├── overlay (backdrop)
└── container (scroll/centering)
    └── content (role="dialog")
        ├── header > icon + title + description + closeButton > closeIcon
        └── body
```

### Data Attributes

- `data-open`, `data-nesting-level`, `data-reduce-motion` on root
- `data-modal-overlay`, `data-modal-container`, `data-modal-content`, `data-modal-header`, `data-modal-body`

### Advanced Pattern: Confirmation dialog

```tsx
<Modal open={open} onOpenChange={setOpen} title="Delete project?" preventOutsideClick closeOnEscape={false} showCloseButton={false}>
  <p>This action cannot be undone.</p>
  <div style={{ display: "flex", justifyContent: "flex-end", gap: 8, marginTop: 16 }}>
    <button onClick={() => setOpen(false)}>Cancel</button>
    <button onClick={handleDelete}>Delete</button>
  </div>
</Modal>
```

---

## MultiSelectDropdown

> A multi-select dropdown with chip display, checkbox options, async loading, and full style customization.

### Import

```tsx
import { MultiSelectDropdown } from "@chumlab/ui/multi-select-dropdown";
import type { MultiSelectOption } from "@chumlab/ui/multi-select-dropdown";
```

### Basic Usage

```tsx
const [selected, setSelected] = useState<string[]>([]);
<MultiSelectDropdown
  options={[{ value: "apple", label: "Apple" }, { value: "banana", label: "Banana" }]}
  value={selected}
  onValueChange={(values) => setSelected(values)}
  placeholder="Select fruits..."
/>
```

### MultiSelectDropdown Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `options` | `MultiSelectOption[]` | `[]` | Available options. |
| `value` | `string[]` | — | Controlled selected values. |
| `defaultValue` | `string[]` | — | Initial values (uncontrolled). |
| `onValueChange` | `(values, options) => void` | — | Selection change callback. |
| `placeholder` | `ReactNode` | — | Trigger placeholder. |
| `label` | `ReactNode` | — | Label. |
| `description` | `ReactNode` | — | Helper text. |
| `disabled` | `boolean` | `false` | Disable component. |
| `required` | `boolean` | `false` | Required field. |
| `error` | `boolean` | `false` | Error state. |
| `errorMessage` | `ReactNode` | — | Error message. |
| `success` | `boolean` | `false` | Success state. |
| `successMessage` | `ReactNode` | — | Success message. |
| `clearable` | `boolean` | `false` | Show clear button. |
| `showChevron` | `boolean` | `true` | Show chevron. |
| `showSelectedChips` | `boolean` | `true` | Chips vs count. |
| `maxDisplayedChips` | `number` | — | Max visible chips. |
| `checkboxIcon` | `ReactNode` | — | Custom checkbox icon. |
| `fullWidth` | `boolean` | `false` | Full width. |
| `loading` | `boolean` | `false` | Loading state. |
| `onLoadOptions` | `() => Promise<Option[]>` | — | Async loader. |
| `loadOnOpen` | `boolean` | `false` | Load on open. |
| `shimmerCount` | `number` | `5` | Shimmer count. |
| `lockScroll` | `boolean` | `false` | Lock body scroll. |
| `dropdownPosition` | `"top" \| "bottom"` | `"bottom"` | Preferred position. |
| `forceDropdownPosition` | `boolean` | `false` | Lock position. |
| `dropdownZIndex` | `number` | `50` | z-index. |
| `dropdownGap` | `number` | `4` | Gap (px). |
| `portalContainer` | `HTMLElement \| null` | `document.body` | Portal target. |
| `keepMounted` | `boolean` | `false` | Keep DOM when closed. |
| `classes` | `MultiSelectDropdownClasses` | — | Per-slot overrides. |
| `unstyled` | `boolean` | `false` | Strip defaults. |
| `name` | `string` | — | Form field name. |
| `open` | `boolean` | — | Controlled open state. |
| `onOpenChange` | `(open) => void` | — | Open callback. |
| `renderTrigger` | `(props) => ReactNode` | — | Custom trigger. |

### Prop Constraints

- `onLoadOptions` requires `loadOnOpen={true}` to trigger.
- `showSelectedChips` default true. Set false for count-only display.
- `maxDisplayedChips` truncates chips with "+N more" badge.

### Class Slots

```
root > label + description + wrapper
└── trigger (combobox)
    ├── chips (chip + chipRemove) or triggerText
    ├── moreCount (+N more)
    ├── clearIcon + chevron
└── content (portal) > optionList (listbox)
    └── option (checkbox + checkboxIcon + label)
```

### Data Attributes

- `data-disabled`, `data-error`, `data-open`, `data-full-width`, `data-success` on root
- `data-placeholder` on trigger when empty

### Advanced Pattern: Async loading

```tsx
<MultiSelectDropdown
  loadOnOpen
  onLoadOptions={() => fetch("/api/options").then(r => r.json())}
  placeholder="Click to load..."
/>
```

---

## MultiSelectSearchableDropdown

> A multi-select dropdown with built-in search, chip display, async search/prefetch, and checkbox options.

### Import

```tsx
import { MultiSelectSearchableDropdown } from "@chumlab/ui/multi-select-searchable-dropdown";
```

### Basic Usage

```tsx
const [selected, setSelected] = useState<string[]>([]);
<MultiSelectSearchableDropdown
  options={[{ value: "apple", label: "Apple" }, { value: "banana", label: "Banana" }]}
  value={selected}
  onValueChange={(values) => setSelected(values)}
  placeholder="Search and select..."
/>
```

### MultiSelectSearchableDropdown Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `options` | `MultiSelectOption[]` | `[]` | Available options. |
| `value` | `string[]` | — | Controlled selected values. |
| `defaultValue` | `string[]` | — | Initial values (uncontrolled). |
| `onValueChange` | `(values, options) => void` | — | Selection change callback. |
| `placeholder` | `ReactNode` | — | Trigger placeholder. |
| `label` | `ReactNode` | — | Label. |
| `description` | `ReactNode` | — | Helper text. |
| `showSearch` | `boolean` | `true` | Show search input. |
| `searchPlaceholder` | `string` | `"Search..."` | Search placeholder. |
| `searchInputAriaLabel` | `string` | — | Search aria-label. |
| `onSearch` | `(query) => Promise<Option[]>` | — | Async search (replaces local filtering). |
| `searchDebounceMs` | `number` | `300` | Search debounce. |
| `initialOptions` | `MultiSelectOption[]` | `[]` | Pre-loaded options. |
| `onLoadInitialOptions` | `() => Promise<Option[]>` | — | Async initial load. |
| `loadInitialOnOpen` | `boolean` | `false` | Load on first open. |
| `loadingText` | `ReactNode` | `"Loading..."` | Loading indicator. |
| `disabled` | `boolean` | `false` | Disable component. |
| `required` | `boolean` | `false` | Required field. |
| `error` | `boolean` | `false` | Error state. |
| `errorMessage` | `ReactNode` | — | Error message. |
| `success` | `boolean` | `false` | Success state. |
| `successMessage` | `ReactNode` | — | Success message. |
| `clearable` | `boolean` | `false` | Show clear button. |
| `showChevron` | `boolean` | `true` | Show chevron. |
| `showSelectedChips` | `boolean` | `true` | Chips vs count. |
| `maxDisplayedChips` | `number` | — | Max visible chips. |
| `checkboxIcon` | `ReactNode` | — | Custom checkbox icon. |
| `fullWidth` | `boolean` | `false` | Full width. |
| `loading` | `boolean` | `false` | Loading state. |
| `lockScroll` | `boolean` | `false` | Lock body scroll. |
| `dropdownPosition` | `"top" \| "bottom"` | `"bottom"` | Preferred position. |
| `forceDropdownPosition` | `boolean` | `false` | Lock position. |
| `dropdownZIndex` | `number` | `50` | z-index. |
| `dropdownGap` | `number` | `4` | Gap (px). |
| `portalContainer` | `HTMLElement \| null` | `document.body` | Portal target. |
| `keepMounted` | `boolean` | `false` | Keep DOM when closed. |
| `classes` | `MultiSelectSearchableDropdownClasses` | — | Per-slot overrides. |
| `unstyled` | `boolean` | `false` | Strip defaults. |

### Prop Constraints

- `onSearch` replaces local filtering when provided.
- `onLoadInitialOptions` requires `loadInitialOnOpen={true}`.
- `searchDebounceMs` only applies to `onSearch`.

### Class Slots

```
root > label + description + wrapper
└── trigger (combobox)
    ├── chips / triggerText / moreCount
    ├── clearIcon + chevron
└── content (portal)
    ├── searchInput > searchIcon + searchInputElement
    └── optionList (listbox) > option (checkbox + label)
```

### Advanced Pattern: Async search with API

```tsx
<MultiSelectSearchableDropdown
  onSearch={async (query) => {
    const res = await fetch(`/api/search?q=${query}`);
    return res.json();
  }}
  searchDebounceMs={300}
  placeholder="Search..."
/>
```

---

## OtpInput

> A one-time password input with per-character fields, keyboard navigation, paste support, grouping, and validation.

### Import

```tsx
import { OtpInput } from "@chumlab/ui/otp-input";
```

### Basic Usage

```tsx
const [otp, setOtp] = useState("");
<OtpInput
  label="Verification Code"
  description="Enter the 6-digit code sent to your phone"
  value={otp}
  onValueChange={setOtp}
/>
```

### OtpInput Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `value` | `string` | — | Controlled value. |
| `onValueChange` | `(value: string) => void` | — | Change callback. |
| `length` | `number` | `6` | Number of input fields. |
| `type` | `"numeric" \| "alphanumeric"` | `"numeric"` | Allowed characters. |
| `groupPattern` | `number[]` | — | Visual grouping (e.g. [3,3]). Sum must equal length. |
| `validate` | `(char: string) => boolean` | — | Custom validation per character. |
| `masked` | `boolean` | `false` | Show dots instead of characters. |
| `label` | `ReactNode` | — | Label above input. |
| `description` | `ReactNode` | — | Helper text. |
| `error` | `boolean` | `false` | Error state. |
| `errorMessage` | `ReactNode` | — | Error message. |
| `disabled` | `boolean` | `false` | Disabled. |
| `classes` | `OtpInputClasses` | — | Per-slot overrides. |
| `unstyled` | `boolean` | `false` | Strip defaults. |

### Prop Constraints

- `groupPattern` array sum must equal `length`.
- `validate` overrides `type` filtering when provided.
- Full keyboard navigation: Arrow keys, Backspace, Delete, Home, End.
- Paste support fills all fields at once.

### Data Attributes

- Each input has `aria-label="Digit N of M"`
- `aria-invalid` set on error state

### Advanced Pattern: Grouped PIN input

```tsx
<OtpInput value={otp} onValueChange={setOtp} groupPattern={[3, 3]} masked />
```

---

## Pagination

> Accessible page navigation with page buttons, rows-per-page selector, custom ellipsis, and section reordering.

### Import

```tsx
import { Pagination } from "@chumlab/ui/pagination";
```

### Basic Usage

```tsx
const [page, setPage] = useState(1);
<Pagination value={page} totalPages={10} onValueChange={setPage} />
```

### Pagination Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `value` | `number` | — | Current page (1-indexed, controlled). |
| `defaultValue` | `number` | `1` | Initial page (uncontrolled). |
| `totalPages` | `number` | **required** | Total pages. |
| `onValueChange` | `(page) => void` | — | Page change callback. |
| `siblingCount` | `number` | `1` | Pages around current. |
| `showRowsPerPage` | `boolean` | `false` | Show rows selector. |
| `rowsPerPage` | `number` | — | Current rows per page. |
| `onRowsPerPageChange` | `(rows) => void` | — | Rows change callback. |
| `rowOptions` | `number[]` | `[5,10,25,50,100]` | Dropdown options. |
| `rowsPerPageLabel` | `string` | `"rows"` | Label after selector. |
| `showLabel` | `string` | `"Show"` | Label before selector. |
| `dropdownAriaLabel` | `string` | `"Rows per page"` | Dropdown ARIA label. |
| `dropdownPosition` | `"top" \| "bottom"` | `"top"` | Vertical placement of the rows-per-page dropdown — opens upward (top) or downward (bottom). |
| `dropdownZIndex` | `number` | `50` | Dropdown z-index. |
| `prevIcon` | `ComponentType \| ReactNode` | Chevron | Previous icon. |
| `nextIcon` | `ComponentType \| ReactNode` | Chevron | Next icon. |
| `dropdownIcon` | `ComponentType \| ReactNode` | Chevron | Dropdown icon. |
| `renderEllipsis` | `(props) => ReactNode` | — | Custom ellipsis renderer. |
| `renderPageInfo` | `(props) => ReactNode` | — | Custom page info renderer. |
| `sectionOrder` | `SectionName[]` | `["selector","pageInfo","nav"]` | Layout order. |
| `disabled` | `boolean` | `false` | Disable all controls. |
| `reduceMotion` | `boolean \| "auto"` | `"auto"` | Motion preference. |
| `classes` | `PaginationClasses` | — | Per-slot overrides. |
| `unstyled` | `boolean` | `false` | Strip defaults. |
| `portalContainer` | `HTMLElement \| null` | `document.body` | Dropdown portal target. |
| `prevAriaLabel` | `string` | `"Previous page"` | Prev button aria-label. |
| `nextAriaLabel` | `string` | `"Next page"` | Next button aria-label. |
| `paginationAriaLabel` | `string` | `"Pagination"` | Nav landmark aria-label. |
| `pageAriaLabel` | `(page) => string` | — | Page button aria-label function. |

### Prop Constraints

- `showRowsPerPage` requires `rowsPerPage` and `onRowsPerPageChange`.
- `renderEllipsis` receives `{ position, onValueChange }`.
- `renderPageInfo` receives `{ value, totalPages, rowsPerPage }`.

### Class Slots

```
root (nav)
├── selector (rows-per-page) > label + selectorButton + selectorDropdown > selectorOption
├── pageInfo
└── nav > navButton(prev) + pageButtons(pageButton/activePageButton + ellipsis) + navButton(next)
```

### Data Attributes

- `data-disabled` — on prev/next at boundary
- `data-active` — on active page button
- `data-selected`, `data-highlighted` — on dropdown options
- `aria-current="page"` — on active page

### Advanced Pattern: Data table pagination

```tsx
const [page, setPage] = useState(1);
const [rows, setRows] = useState(10);
<Pagination
  value={page}
  totalPages={Math.ceil(totalItems / rows)}
  onValueChange={setPage}
  showRowsPerPage
  rowsPerPage={rows}
  onRowsPerPageChange={(r) => { setRows(r); setPage(1); }}
/>
```

---

## RadioButton

> A radio button group with labels, descriptions, custom icons, sizes, and orientations.

### Import

```tsx
import { RadioGroup, RadioButton } from "@chumlab/ui/radio-button";
```

### Basic Usage

```tsx
<RadioGroup name="plan" defaultValue="basic">
  <RadioButton value="basic" label="Basic" />
  <RadioButton value="pro" label="Pro" />
  <RadioButton value="enterprise" label="Enterprise" />
</RadioGroup>
```

### RadioGroup Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `name` | `string` | **required** | Form field name. |
| `value` | `string` | — | Controlled value. |
| `defaultValue` | `string` | — | Initial value (uncontrolled). |
| `onValueChange` | `(value: string) => void` | — | Change callback. |
| `orientation` | `"horizontal" \| "vertical"` | `"vertical"` | Layout direction. |
| `disabled` | `boolean` | `false` | Disable all. |
| `error` | `boolean` | `false` | Error state. |
| `errorMessage` | `ReactNode` | — | Error message. |
| `classes` | `RadioGroupClasses` | — | Per-slot overrides. |
| `unstyled` | `boolean` | `false` | Strip defaults. |

### RadioButton Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `value` | `string` | **required** | Option value. |
| `label` | `ReactNode` | — | Display text. |
| `description` | `ReactNode` | — | Helper text. |
| `disabled` | `boolean` | `false` | Disable this option. |
| `size` | `"sm" \| "md" \| "lg"` | `"md"` | Size preset. |
| `checkedIcon` | `ReactNode` | — | Custom checked icon. |
| `uncheckedIcon` | `ReactNode` | — | Custom unchecked icon. |
| `classes` | `RadioButtonClasses` | — | Per-slot overrides. |

### Data Attributes

- `role="radiogroup"` on wrapper
- Native `<input type="radio">` — browser handles arrow key navigation

### Advanced Pattern: Horizontal card selection

```tsx
<RadioGroup name="tier" orientation="horizontal">
  <RadioButton value="free" label="Free" description="$0/mo" />
  <RadioButton value="pro" label="Pro" description="$19/mo" />
</RadioGroup>
```

---

## ResizablePanel

> A draggable resizable panel with constraints, keyboard resizing, and Escape to cancel.

### Import

```tsx
import { ResizablePanel } from "@chumlab/ui/resizable-panel";
```

### Basic Usage

```tsx
<ResizablePanel defaultValue={300} minValue={150} maxValue={500}>
  <div style={{ padding: 16 }}>Drag the edge to resize.</div>
</ResizablePanel>
```

### ResizablePanel Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `value` | `number` | — | Controlled size (px). |
| `defaultValue` | `number` | — | Initial size (uncontrolled). |
| `onValueChange` | `(value: number) => void` | — | Size change callback. |
| `minValue` | `number` | — | Minimum size. |
| `maxValue` | `number` | — | Maximum size. |
| `direction` | `"right" \| "left" \| "top" \| "bottom"` | `"right"` | Resize direction. |
| `step` | `number` | `10` | Keyboard resize step (px). |
| `disabled` | `boolean` | `false` | Disable resizing. |
| `onResizeStart` | `() => void` | — | Drag start callback. |
| `onResizeEnd` | `(value: number) => void` | — | Drag end callback. |
| `classes` | `ResizablePanelClasses` | — | Per-slot overrides. |
| `unstyled` | `boolean` | `false` | Strip defaults. |

### Data Attributes

- `role="separator"` on drag handle
- `aria-valuenow`, `aria-valuemin`, `aria-valuemax` for current size
- `aria-orientation` for resize direction
- Arrow keys resize, Home/End for min/max, Escape to cancel

### Advanced Pattern: Controlled sidebar

```tsx
const [width, setWidth] = useState(250);
<ResizablePanel value={width} onValueChange={setWidth} minValue={200} maxValue={400} direction="right">
  <nav>Sidebar content</nav>
</ResizablePanel>
```

---

## SearchableDropdown

> A single-select dropdown with built-in search input, async search/prefetch, portal rendering, and keyboard navigation.

### Import

```tsx
import { SearchableDropdown } from "@chumlab/ui/searchable-dropdown";
```

### Basic Usage

```tsx
const [value, setValue] = useState<string | null>(null);
<SearchableDropdown
  options={[{ value: "apple", label: "Apple" }, { value: "banana", label: "Banana" }]}
  value={value}
  onValueChange={(v) => setValue(v)}
  placeholder="Search fruits..."
/>
```

### SearchableDropdown Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `options` | `SearchableDropdownOption[]` | `[]` | Available options. |
| `value` | `string \| null` | — | Controlled value. |
| `defaultValue` | `string` | — | Initial value (uncontrolled). |
| `onValueChange` | `(value, option) => void` | — | Selection callback. |
| `placeholder` | `ReactNode` | — | Trigger placeholder. |
| `label` | `ReactNode` | — | Label. |
| `description` | `ReactNode` | — | Helper text. |
| `showSearch` | `boolean` | `true` | Show search input. |
| `searchPlaceholder` | `string` | `"Search..."` | Search placeholder. |
| `searchInputAriaLabel` | `string` | `"Search options"` | Search aria-label. |
| `onSearch` | `(query) => Promise<Option[]>` | — | Async search (replaces local). |
| `searchDebounceMs` | `number` | `300` | Debounce delay. |
| `initialOptions` | `Option[]` | `[]` | Pre-loaded options for async. |
| `onLoadInitialOptions` | `() => Promise<Option[]>` | — | Async initial load. |
| `loadInitialOnOpen` | `boolean` | `false` | Load on first open. |
| `disabled` | `boolean` | `false` | Disabled. |
| `required` | `boolean` | `false` | Required. |
| `error` | `boolean` | `false` | Error state. |
| `errorMessage` | `ReactNode` | — | Error message. |
| `success` | `boolean` | `false` | Success state. |
| `successMessage` | `ReactNode` | — | Success message. |
| `clearable` | `boolean` | `false` | Show clear button. |
| `showChevron` | `boolean` | `true` | Show chevron. |
| `showSelectedIcon` | `boolean` | `true` | Check icon on selected. |
| `selectedIcon` | `ReactNode` | — | Custom selected icon. |
| `fullWidth` | `boolean` | `false` | Full width. |
| `loading` | `boolean` | `false` | Loading state. |
| `shimmerCount` | `number` | `5` | Shimmer count. |
| `lockScroll` | `boolean` | `false` | Lock body scroll. |
| `dropdownPosition` | `"top" \| "bottom"` | `"bottom"` | Position. |
| `forceDropdownPosition` | `boolean` | `false` | Lock position. |
| `dropdownZIndex` | `number` | `50` | z-index. |
| `dropdownGap` | `number` | `4` | Gap (px). |
| `portalContainer` | `HTMLElement \| null` | `document.body` | Portal target. |
| `keepMounted` | `boolean` | `false` | Keep DOM when closed. |
| `typeaheadTimeout` | `number` | `500` | Type-ahead buffer reset (ms). |
| `classes` | `SearchableDropdownClasses` | — | Per-slot overrides. |
| `unstyled` | `boolean` | `false` | Strip defaults. |
| `name` | `string` | — | Form field name. |
| `open` | `boolean` | — | Controlled open state. |
| `onOpenChange` | `(open) => void` | — | Open callback. |
| `renderTrigger` | `(props) => ReactNode` | — | Custom trigger. |
| `loadingText` | `ReactNode` | — | Custom loading indicator. |

### Prop Constraints

- `onSearch` replaces local filtering when provided.
- `onLoadInitialOptions` requires `loadInitialOnOpen={true}`.
- `renderTrigger` must spread all provided props and attach `ref`.

### Class Slots

```
root > label + description + wrapper
├── trigger (combobox) > triggerText + clearIcon + chevron
└── content (portal)
    ├── searchInput > searchIcon + searchInputElement
    ├── optionList (listbox) > option [checkIcon]
    ├── noResults
    └── shimmer > shimmerItem
```

### Data Attributes

- `data-disabled`, `data-error`, `data-open`, `data-success` on root
- `data-selected`, `data-focused`, `data-disabled` on options
- `data-position="top|bottom"` on dropdown panel

### Advanced Pattern: Async search with API

```tsx
<SearchableDropdown
  onSearch={async (query) => {
    const res = await fetch(`/api/search?q=${query}`);
    return res.json();
  }}
  searchDebounceMs={300}
  placeholder="Search..."
/>
```

---

## Slider

> A range slider with single and dual-thumb modes, marks, tooltips, vertical orientation, and full keyboard navigation.

### Import

```tsx
import { Slider } from "@chumlab/ui/slider";
```

### Basic Usage

```tsx
const [value, setValue] = useState(50);
<Slider label="Volume" value={value} onValueChange={(v) => setValue(v as number)} />
```

### Slider Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `value` | `number \| [number, number]` | — | Controlled value. |
| `defaultValue` | `number \| [number, number]` | — | Initial value. |
| `onValueChange` | `(value) => void` | — | Change callback. |
| `min` | `number` | `0` | Minimum value. |
| `max` | `number` | `100` | Maximum value. |
| `step` | `number` | `1` | Step size. |
| `marks` | `{ value: number; label?: string }[]` | — | Dots on track. |
| `transitionDuration` | `number` | `200` | Duration in ms of the click-to-jump animation when the user clicks a mark/track. Set to `0` to disable. Suppressed automatically during an active drag. |
| `showTooltip` | `"always" \| "hover" \| false` | — | Value tooltip above thumb. |
| `orientation` | `"horizontal" \| "vertical"` | `"horizontal"` | Layout direction. |
| `disabled` | `boolean` | `false` | Disabled. |
| `label` | `ReactNode` | — | Label above slider. |
| `error` | `boolean` | `false` | Error state. |
| `success` | `boolean` | `false` | Success state. |
| `classes` | `SliderClasses` | — | Per-slot overrides. |
| `unstyled` | `boolean` | `false` | Strip defaults. |

### Prop Constraints

- `value` as `number` for single, `[number, number]` for range.
- `min` must be less than `max`.

### Data Attributes

- `role="slider"` on thumbs with `aria-valuenow`, `aria-valuemin`, `aria-valuemax`
- Arrow keys (small step), Shift+Arrow (large step), Home/End, PageUp/PageDown

### Advanced Pattern: Range slider

```tsx
const [range, setRange] = useState<[number, number]>([20, 80]);
<Slider value={range} onValueChange={(v) => setRange(v as [number, number])} marks={[{ value: 0, label: "0%" }, { value: 100, label: "100%" }]} />
```

---

## Stepper

> A step progress indicator with numbered/icon/dot variants, horizontal/vertical layouts, clickable steps, and tooltips.

### Import

```tsx
import { Stepper } from "@chumlab/ui/stepper";
import type { Step } from "@chumlab/ui/stepper";
```

### Basic Usage

```tsx
const [active, setActive] = useState<string | number>(1);
<Stepper
  steps={[{ id: 1, label: "Account" }, { id: 2, label: "Profile" }, { id: 3, label: "Review" }]}
  value={active}
  onValueChange={setActive}
  isStepClickable={() => true}
/>
```

### Stepper Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `steps` | `Step[]` | **required** | Step definitions. |
| `value` | `string \| number` | — | Active step (controlled). |
| `defaultValue` | `string \| number` | — | Initial step (uncontrolled). |
| `onValueChange` | `(stepId) => void` | — | Step change callback. |
| `beforeStepChange` | `(next, current) => boolean` | — | Guard before navigation. |
| `orientation` | `"horizontal" \| "vertical"` | `"horizontal"` | Layout. |
| `variant` | `"numbered" \| "icon" \| "dot"` | `"numbered"` | Indicator style. |
| `activationMode` | `"automatic" \| "manual"` | `"manual"` | Keyboard activation. |
| `isStepClickable` | `(id, status) => boolean` | completed+active | Click guard. |
| `getStepStatus` | `(id, index, activeIndex) => StepStatus` | — | Custom status logic. |
| `showLabels` | `boolean` | `true` | Show step labels. |
| `showDescriptions` | `boolean` | `false` | Show descriptions. |
| `showConnectors` | `boolean` | `true` | Show connecting lines. |
| `labelPosition` | `"bottom" \| "right"` | `"right"` | Label placement (horizontal only). |
| `completedIcon` | `ComponentType \| ReactNode` | Check | Completed icon. |
| `errorIcon` | `ComponentType \| ReactNode` | X | Error icon. |
| `fullWidth` | `boolean` | `false` | Full width. |
| `showTooltips` | `boolean` | `false` | Enable tooltips. |
| `tooltipDefaults` | `StepperTooltipDefaults` | — | Default tooltip config. |
| `indicatorSize` | `number` | per variant | Indicator px size. |
| `disabled` | `boolean` | `false` | Disable all. |
| `loop` | `boolean` | `false` | Loop navigation. |
| `renderStep` | `(props, defaultEl) => ReactNode` | — | Custom step renderer. |
| `classes` | `StepperClasses` | — | Per-slot overrides. |
| `unstyled` | `boolean` | `false` | Strip defaults. |
| `reduceMotion` | `boolean \| "auto"` | `"auto"` | Motion preference. |
| `aria-label` | `string` | `"Progress"` | Nav landmark label. |

### Step (data shape)

| Prop | Type | Description |
|------|------|-------------|
| `id` | `string \| number` | **Required.** Unique identifier. |
| `label` | `string` | **Required.** Display text. |
| `description` | `string` | Optional description text. |
| `icon` | `ComponentType` | Icon component (for "icon" variant). |
| `tooltip` | `ReactNode \| TooltipConfig` | Tooltip content. |
| `disabled` | `boolean` | Disable this step. |

### Prop Constraints

- `isStepClickable` controls which steps are interactive. Default: completed + active.
- `beforeStepChange` return false to prevent navigation.
- `getStepStatus` overrides default status logic. Must handle all steps.
- `labelPosition` only applies to horizontal orientation.

### Class Slots

```
root (nav) > list (ol)
├── stepContainer (li) > step (button/div)
│   ├── indicator > indicatorIcon
│   └── labelWrapper > label + description
├── connector > connectorHorizontal/connectorVertical
```

Status-specific slots: `indicatorActive`, `indicatorCompleted`, `indicatorError`, `indicatorPending`, `labelActive`, `labelCompleted`, `descriptionActive`, `stepInteractive`, `stepDisabled`.

### Data Attributes

- `data-orientation`, `data-variant`, `data-disabled` on root
- `data-status="pending|active|completed|error"` on steps
- `aria-current="step"` on active step

### Advanced Pattern: Error state with custom status

```tsx
<Stepper
  steps={steps}
  getStepStatus={(id, index, activeIndex) => {
    if (index === 1) return "error";
    if (index < activeIndex) return "completed";
    if (index === activeIndex) return "active";
    return "pending";
  }}
/>
```

---

## Switch

> A toggle switch for binary on/off states with labels, validation, loading, custom icons, and CSS custom properties.

### Import

```tsx
import { Switch } from "@chumlab/ui/switch";
```

### Basic Usage

```tsx
const [enabled, setEnabled] = useState(false);
<Switch label="Email notifications" checked={enabled} onValueChange={setEnabled} />
```

### Switch Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `label` | `ReactNode` | — | Label text. |
| `description` | `ReactNode` | — | Helper text. |
| `checked` | `boolean` | — | Controlled checked state. |
| `defaultChecked` | `boolean` | — | Initial state (uncontrolled). |
| `onValueChange` | `(checked: boolean) => void` | — | Change callback. |
| `disabled` | `boolean` | `false` | Disabled. |
| `required` | `boolean` | `false` | Required. |
| `error` | `boolean` | `false` | Error state. |
| `errorMessage` | `ReactNode` | — | Error message. |
| `success` | `boolean` | `false` | Success state. |
| `successMessage` | `ReactNode` | — | Success message. |
| `loading` | `boolean` | `false` | Loading state. |
| `size` | `"sm" \| "md" \| "lg"` | `"md"` | Size preset. |
| `checkedIcon` | `ReactNode` | — | Icon inside thumb when checked. |
| `uncheckedIcon` | `ReactNode` | — | Icon inside thumb when unchecked. |
| `transitionDuration` | `number` | — | Animation duration (ms). |
| `reduceMotion` | `boolean \| "auto"` | `"auto"` | Motion preference. |
| `classes` | `SwitchClasses` | — | Per-slot overrides. |
| `unstyled` | `boolean` | `false` | Strip defaults. |
| `name` | `string` | — | Form field name. |

### Prop Constraints

- `checked` + `onValueChange` for controlled. Never combine with `defaultChecked`.
- `transitionDuration` set to 0ms when motion reduced.

### CSS Custom Properties

- `--switch-width`, `--switch-height` — overall dimensions
- `--switch-thumb-size` — thumb diameter
- `--switch-thumb-offset` — thumb inset from track edge
- `--switch-checked-bg`, `--switch-unchecked-bg` — track colors

### Data Attributes

- `role="switch"` with `aria-checked`
- `aria-disabled`, `aria-required`, `aria-describedby`
- Keyboard: Space/Enter toggles, Tab focuses

### Advanced Pattern: Switch with icons

```tsx
<Switch
  checked={darkMode}
  onValueChange={setDarkMode}
  checkedIcon={<MoonIcon className="w-3 h-3" />}
  uncheckedIcon={<SunIcon className="w-3 h-3" />}
/>
```

---

## TabPanel

> Accessible tab panel with icons, badges, tooltips, closable tabs, vertical orientation, and lazy loading.

### Import

```tsx
import { TabPanel } from "@chumlab/ui/tab-panel";
import type { Tab } from "@chumlab/ui/tab-panel";
```

### Basic Usage

```tsx
<TabPanel
  tabs={[{ id: "home", label: "Home" }, { id: "profile", label: "Profile" }, { id: "settings", label: "Settings" }]}
  defaultValue="home"
  renderContent={(tab) => <div style={{ padding: 16 }}>{tab.label} content</div>}
/>
```

### TabPanel Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `tabs` | `Tab[]` | **required** | Tab definitions. |
| `value` | `string` | — | Active tab (controlled). |
| `defaultValue` | `string` | — | Initial tab (uncontrolled). |
| `onValueChange` | `(tabId) => void` | — | Tab change callback. |
| `renderContent` | `(tab) => ReactNode` | — | Content renderer. |
| `orientation` | `"horizontal" \| "vertical"` | `"horizontal"` | Layout. |
| `activationMode` | `"automatic" \| "manual"` | `"automatic"` | Keyboard activation. |
| `closable` | `boolean` | `false` | Show close buttons. |
| `onClose` | `(tabId) => void` | — | Close tab callback. |
| `showTooltips` | `boolean` | `false` | Enable tooltips. |
| `tooltipDefaults` | `TooltipDefaults` | — | Default tooltip config. |
| `lazyMount` | `boolean` | `false` | Lazy-mount panels. |
| `keepMounted` | `boolean` | `false` | Keep inactive panels. |
| `forceMount` | `boolean` | `false` | Mount all panels. |
| `loop` | `boolean` | `true` | Loop keyboard navigation. |
| `disabled` | `boolean` | `false` | Disable all. |
| `renderTab` | `(props, defaultEl) => ReactNode` | — | Custom tab renderer. |
| `classes` | `TabPanelClasses` | — | Per-slot overrides. |
| `unstyled` | `boolean` | `false` | Strip defaults. |
| `aria-label` | `string` | `"Tabs"` | Tablist label. |
| `aria-labelledby` | `string` | — | External labelling element. |

### Tab (data shape)

| Prop | Type | Description |
|------|------|-------------|
| `id` | `string` | **Required.** Unique identifier. |
| `label` | `string` | **Required.** Display text. |
| `icon` | `ComponentType` | Tab icon component. |
| `iconPosition` | `"left" \| "right"` | Icon placement. |
| `count` | `number` | Badge count. |
| `disabled` | `boolean` | Disable this tab. |
| `tooltip` | `ReactNode \| TooltipConfig` | Tooltip content. |

### Prop Constraints

- `closable` requires `onClose` handler.
- `renderContent` is required for content rendering.
- `lazyMount` only mounts panels on first activation.

### Class Slots

```
root
├── tabList (role="tablist", overflow-x-auto)
│   └── tab (role="tab") > icon + label + count + indicator
└── panel (role="tabpanel")
```

Tab-specific slots: `tab`, `tabActive`, `tabInactive`, `tabDisabled`, `tabFocus`, `label`, `labelActive`, `labelInactive`, `icon`, `iconActive`, `iconInactive`, `count`, `countActive`, `countInactive`, `indicator`, `panel`.

### Data Attributes

- `data-state="active|inactive"` on tabs
- `data-disabled` on disabled tabs
- `data-orientation` on tablist and root

### Advanced Pattern: Closable dynamic tabs

```tsx
const [tabs, setTabs] = useState(initialTabs);
<TabPanel tabs={tabs} closable onClose={(id) => setTabs(tabs.filter(t => t.id !== id))} renderContent={...} />
```

---

## Table

> A feature-rich data table with sorting, filtering, selection, pinned columns, row expansion, drag-and-drop, inline editing, column resizing, infinite scroll, and CSV export. Built on TanStack Table.

### Import

```tsx
import { Table } from "@chumlab/ui/table";
import type { ColumnDef } from "@tanstack/react-table";
```

### Basic Usage

```tsx
interface User { name: string; email: string; }
const columns: ColumnDef<User>[] = [
  { accessorKey: "name", header: "Name" },
  { accessorKey: "email", header: "Email" },
];
<Table columns={columns} data={data} />
```

### Table Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `columns` | `ColumnDef<TData>[]` | **required** | Column definitions. |
| `data` | `TData[]` | **required** | Row data. |
| `sortable` | `boolean` | `false` | Enable column sorting. |
| `sorting` | `SortingState` | — | Controlled sorting state. |
| `onSortingChange` | `(sorting) => void` | — | Sort change callback. |
| `manualSorting` | `boolean` | `false` | Server-side sorting. |
| `selectable` | `boolean` | `false` | Enable row selection. |
| `onSelectionChange` | `(rows) => void` | — | Selection callback. |
| `pinnedColumns` | `string[]` | — | Column IDs to pin left. |
| `onPinColumn` | `(id, pinned) => void` | — | Pin toggle callback. |
| `expandable` | `boolean` | `false` | Enable row expansion. |
| `renderExpandedRow` | `(row) => ReactNode` | — | Expanded row content. |
| `draggable` | `boolean` | `false` | Enable drag-and-drop. |
| `onReorder` | `(data) => void` | — | Reorder callback. |
| `editable` | `boolean` | `false` | Enable inline editing. |
| `onCellEdit` | `(rowIndex, columnId, value) => void` | — | Cell edit callback. |
| `resizable` | `boolean` | `false` | Column resize handles. |
| `stickyHeader` | `boolean` | `false` | Sticky header (needs maxHeight). |
| `maxHeight` | `string \| number` | — | Max table height. |
| `striped` | `boolean` | `false` | Alternating row colors. |
| `density` | `"compact" \| "normal" \| "comfortable"` | `"normal"` | Row density. |
| `globalSearch` | `boolean` | `false` | Built-in search bar. |
| `onRowClick` | `(row) => void` | — | Row click handler. |
| `infiniteScroll` | `boolean` | `false` | Enable infinite scroll. |
| `onLoadMore` | `() => void` | — | Load more callback. |
| `hasMore` | `boolean` | — | More data available. |
| `loading` | `boolean` | `false` | Loading state (shimmer). |
| `emptyContent` | `ReactNode` | — | Empty state content. |
| `classes` | `TableClasses` | — | Per-slot overrides. |
| `unstyled` | `boolean` | `false` | Strip defaults. |

### Prop Constraints

- `expandable` + `renderExpandedRow` both needed for row expansion.
- `draggable` + `onReorder` both needed for drag-and-drop.
- `editable` + `onCellEdit` both needed for inline editing.
- `infiniteScroll` + `onLoadMore` + `hasMore` all three needed.
- `stickyHeader` requires `maxHeight` to be meaningful.
- `manualSorting` — table won't sort locally when true.

### Class Slots

Key slots: `container`, `table`, `headerRow`, `headerCell`, `row`, `cell`, `selectedRow`, `pinnedContainer`, `pinnedTable`, `unpinnedContainer`, `empty`, `shimmer`, `shimmerRow`, `shimmerCell`, `shimmerBar`, `searchBar`, `searchInput`.

### Data Attributes

- `data-clickable` on rows with onRowClick
- `data-striped` on alternating rows
- `data-selected`, `data-dragging`, `data-expanded` on rows
- `role="columnheader"` with `aria-sort` on sortable headers

### Advanced Pattern: Sortable table with pagination

```tsx
const [sorting, setSorting] = useState<SortingState>([]);
const [page, setPage] = useState(1);
<Table columns={columns} data={paginatedData} sortable sorting={sorting} onSortingChange={setSorting} />
<Pagination value={page} totalPages={totalPages} onValueChange={setPage} />
```

---

## TextArea

> A production-grade multi-line text input with auto-resize, character count, validation states, icons, and form integration.

### Import

```tsx
import { TextArea } from "@chumlab/ui/textarea";
```

### Basic Usage

```tsx
<TextArea label="Message" placeholder="Write your message..." />
<TextArea label="Bio" placeholder="Tell us about yourself..." maxLength={200} showCount />
```

### TextArea Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `label` | `ReactNode` | — | Label above textarea. |
| `description` | `ReactNode` | — | Helper text. |
| `placeholder` | `string` | — | Placeholder. |
| `rows` | `number` | — | Initial visible rows. |
| `error` | `boolean` | `false` | Error state. |
| `errorMessage` | `ReactNode` | — | Error message. |
| `success` | `boolean` | `false` | Success state. |
| `successMessage` | `ReactNode` | — | Success message. |
| `size` | `TextAreaSize` | — | Visual size (data attribute). |
| `startIcon` | `ReactNode` | — | Icon before textarea. |
| `endIcon` | `ReactNode` | — | Icon after textarea. |
| `onStartIconClick` | `() => void` | — | Makes startIcon a button. |
| `onEndIconClick` | `() => void` | — | Makes endIcon a button. |
| `startIconLabel` | `string` | — | Aria label for startIcon. |
| `endIconLabel` | `string` | — | Aria label for endIcon. |
| `onValueChange` | `(value: string) => void` | — | Value callback. |
| `clearable` | `boolean` | `false` | Show clear button. |
| `showCount` | `boolean` | `false` | Show character count. |
| `autoResize` | `boolean` | `false` | Auto-grow with content. |
| `loading` | `boolean` | `false` | Loading state. |
| `fullWidth` | `boolean` | `false` | Full width. |
| `classes` | `TextAreaClasses` | — | Per-slot overrides. |
| `unstyled` | `boolean` | `false` | Strip defaults. |
| `maxLength` | `number` | — | Max characters. |
| `required` | `boolean` | — | Required. |
| `disabled` | `boolean` | — | Disabled. |
| `readOnly` | `boolean` | — | Read-only. |

### Prop Constraints

- `showCount` requires `maxLength`.
- `autoResize` overrides manual resize CSS. Sets overflow: hidden and resize: none.

### Class Slots

```
root > label + description + wrapper(startIcon + textarea + endIcon + clear + loader) + error/success + count
```

### Data Attributes

- `data-size`, `data-disabled`, `data-error`, `data-success`, `data-readonly`

### Advanced Pattern: Auto-resize with character count

```tsx
<TextArea autoResize rows={2} label="Notes" showCount maxLength={500} placeholder="Start typing..." />
```

---

## TimePicker

> A time input with dropdown list or analog clock variant, smart parsing, 12/24-hour formats, min/max constraints, and clearable.

### Import

```tsx
import { TimePicker } from "@chumlab/ui/time-picker";
```

### Basic Usage

```tsx
const [time, setTime] = useState<string | null>(null);
<TimePicker value={time} onValueChange={(t) => setTime(t)} />
```

### TimePicker Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `value` | `string \| null` | — | Controlled value (HH:MM format). |
| `defaultValue` | `string` | — | Initial value. |
| `onValueChange` | `(time: string \| null) => void` | — | Change callback. |
| `variant` | `"dropdown" \| "clock"` | `"dropdown"` | UI variant. |
| `format` | `"24h" \| "12h"` | `"24h"` | Time format. |
| `minuteStep` | `number` | `1` | Minute interval (1-60). |
| `minTime` | `string` | — | Earliest selectable (HH:MM). |
| `maxTime` | `string` | — | Latest selectable (HH:MM). |
| `placeholder` | `string` | — | Placeholder. |
| `label` | `ReactNode` | — | Label. |
| `description` | `ReactNode` | — | Helper text. |
| `disabled` | `boolean` | `false` | Disabled. |
| `error` | `boolean` | `false` | Error state. |
| `errorMessage` | `ReactNode` | — | Error message. |
| `success` | `boolean` | `false` | Success state. |
| `successMessage` | `ReactNode` | — | Success message. |
| `required` | `boolean` | `false` | Required. |
| `clearable` | `boolean` | `false` | Show clear button. |
| `loading` | `boolean` | `false` | Loading state. |
| `fullWidth` | `boolean` | `false` | Full width. |
| `open` | `boolean` | — | Controlled open state. |
| `onOpenChange` | `(open: boolean) => void` | — | Open callback. |
| `lockScroll` | `boolean` | `false` | Lock body scroll. |
| `dropdownPosition` | `"top" \| "bottom"` | `"bottom"` | Position. |
| `forceDropdownPosition` | `boolean` | `false` | Lock position. |
| `dropdownZIndex` | `number` | — | z-index. |
| `keepMounted` | `boolean` | `false` | Keep DOM when closed (dropdown only). |
| `onConfirm` | `() => void` | — | Clock OK callback. |
| `onCancel` | `() => void` | — | Clock cancel callback. |
| `classes` | `TimePickerClasses` | — | Per-slot overrides. |
| `unstyled` | `boolean` | `false` | Strip defaults. |
| `name` | `string` | — | Form field name. |
| `reduceMotion` | `boolean \| "auto"` | `"auto"` | Motion preference. |

### Prop Constraints

- `variant` "dropdown" shows a list; "clock" shows analog clock face.
- `minuteStep` clamped to 1-60. Invalid values auto-corrected.
- `minTime` / `maxTime` format: "HH:MM".
- Clock variant always remounts on close to reset selection mode.

### Class Slots

Key slots: `root`, `label`, `trigger`, `input`, `endIcon`, `clearIcon`, `dropdown`, `optionList`, `option`, `optionSelected`, `optionFocused`, `selectedIcon`, `error`, `description`, `clockContainer`, `clockFace`, `clockNumber`, `clockNumberSelected`, `clockHand`, `clockHandLine`, `clockHandDot`, `clockPeriodButton`, `clockPeriodActive`, `clockOkButton`, `clockCancelButton`.

### Data Attributes

- `data-disabled`, `data-error`, `data-open` on trigger
- `data-selected`, `data-focused` on options
- `data-active` on clock buttons
- `data-position="top|bottom"` on dropdown

### Advanced Pattern: 12-hour with constraints

```tsx
<TimePicker format="12h" minTime="09:00" maxTime="17:00" minuteStep={30} label="Meeting Time" />
```

---

## Toast

> A notification system for brief messages. 4 types, 6 positions, progress bars, auto-dismiss.

### Import

```tsx
import { ToastProvider, useToast } from "@chumlab/ui/toast";
```

### Basic Usage

```tsx
function App() {
  return <ToastProvider><MyComponent /></ToastProvider>;
}
function MyComponent() {
  const toast = useToast();
  return <button onClick={() => toast.success("Saved!")}>Save</button>;
}
```

### ToastProvider Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `position` | `ToastPosition` | `"top-right"` | 6 positions: top-right, top-left, top-center, bottom-right, bottom-left, bottom-center. |
| `maxToasts` | `number` | — | Limit visible toasts. |
| `classes` | `ToastClasses` | — | Per-slot overrides. |
| `unstyled` | `boolean` | `false` | Strip defaults. |

### Toast API Methods

```tsx
const toast = useToast();
toast.success("message", options?);
toast.warning("message", options?);
toast.error("message", options?);
toast.info("message", options?);
toast.toast({ type, message, ...options }); // generic
toast.dismiss(id);
toast.dismissAll();
```

### Toast Options

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `type` | `"success" \| "warning" \| "error" \| "info"` | — | Toast type. |
| `message` | `string` | — | Toast message. |
| `description` | `string` | — | Additional text. |
| `duration` | `number` | varies | Auto-dismiss (ms). 0 = persistent. |
| `showProgress` | `boolean` | `false` | Progress bar. |
| `icon` | `ReactNode` | — | Custom icon. |
| `content` | `ReactNode` | — | Custom content (replaces default). |
| `onDismiss` | `() => void` | — | Dismiss callback. |

### Prop Constraints

- `duration` 0 = persistent toast. Default varies by type.
- `content` replaces the default message/description layout entirely.

### CSS Custom Properties

- `--toast-success-bg`, `--toast-success-border`, `--toast-success-text`
- Same pattern for warning, error, info, default

### Class Slots

`container`, `content`, `message`, `description`, `progress`, `closeButton`, `icon`

### Data Attributes

- `role="alert"` for error/warning, `role="status"` for success/info
- `aria-live="assertive"` for alerts, `"polite"` for status
- `aria-atomic="true"` for full toast announcement

### Advanced Pattern: Toast with progress bar

```tsx
toast.success("File uploaded", { showProgress: true, duration: 5000, description: "photo.jpg (2.4 MB)" });
```

---

## Tooltip

> Accessible tooltip with configurable positioning, delays, rich content, arrows, and auto-truncation.

### Import

```tsx
import { Tooltip } from "@chumlab/ui/tooltip";
```

### Basic Usage

```tsx
<Tooltip content="Save your changes">
  <button>Hover me</button>
</Tooltip>
```

### Tooltip Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `content` | `ReactNode` | — | Tooltip content. |
| `side` | `"top" \| "right" \| "bottom" \| "left"` | `"top"` | Placement side. |
| `align` | `"start" \| "center" \| "end"` | `"center"` | Alignment. |
| `sideOffset` | `number` | `8` | Distance from trigger. |
| `alignOffset` | `number` | `0` | Alignment offset. |
| `maxWidth` | `string \| number` | — | Max tooltip width. |
| `wordWrap` | `"normal" \| "break-word" \| "nowrap"` | `"normal"` | Text wrapping. |
| `delayDuration` | `number` | `200` | Show delay (ms). |
| `hideDelayDuration` | `number` | `0` | Hide delay (ms). |
| `showArrow` | `boolean` | `true` | Show pointing arrow. |
| `arrowSize` | `number` | `8` | Arrow size (px). |
| `arrowColor` | `string` | — | Arrow fill color. |
| `disabled` | `boolean` | `false` | Disable tooltip. |
| `truncate` | `boolean` | `false` | Auto-truncate trigger text. |
| `shadow` | `TooltipShadow` | `"md"` | Shadow preset or custom. |
| `open` | `boolean` | — | Controlled open state. |
| `onOpenChange` | `(open) => void` | — | Open callback. |
| `asChild` | `boolean` | `false` | Merge props onto child. |
| `portal` | `boolean` | `true` | Render in portal. |
| `classes` | `TooltipClasses` | — | Per-slot overrides. |
| `unstyled` | `boolean` | `false` | Strip defaults. |
| `zIndex` | `number` | — | Custom z-index. |

### Prop Constraints

- `truncate` auto-truncates trigger text, shows full text in tooltip on hover.
- `asChild` merges trigger props onto child element instead of wrapping.
- `arrowColor` should match tooltip `content` background.

### Class Slots

```
trigger (span wrapper or merged via asChild)
└── portal > content (role="tooltip")
    ├── children (tooltip text/JSX)
    └── arrow
```

Slots: `trigger`, `content`, `arrow`, `baseArrow`.

### Data Attributes

- `role="tooltip"` on content
- `aria-describedby` links trigger to tooltip
- Keyboard: shows on focus, hides on blur/Escape

### Advanced Pattern: Rich tooltip with custom positioning

```tsx
<Tooltip
  content={<div><strong>Keyboard shortcut</strong><br/>Ctrl + S</div>}
  side="bottom"
  align="start"
  maxWidth={200}
  delayDuration={500}
>
  <button>Save</button>
</Tooltip>
```
